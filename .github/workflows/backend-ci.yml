name: Backend CI

# 分支保护 required check 名为 "Backend CI / backend"。⚠️ 禁止在 on.pull_request 配 paths
# 过滤：只改 .github/** 的 PR（如 Dependabot 升级 action）会让本 workflow 不触发，
# required check 永不报告 → PR 永久 BLOCKED。恒触发 + 步骤内 paths-filter 门控：
# 无后端变更时 JDK/verify 步骤被跳过，job 照常运行并报告 → required check 稳定通过。
on:
  push:
    branches:
      - develop
      - main
    paths:
      - easyorange-backend/**
  pull_request:
    branches:
      - develop
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # 后端门禁：一次 verify 完成 11 模块单测 + JaCoCo（80% 行/60% 分支）+ Spotless 格式 +
  # 跨模块 optional 依赖校验（TD-011）。唯一 merge-freeze 后端闸门。OWASP / PIT 慢扫描为
  # 独立 schedule workflow（security-scan.yml / pit-mutation.yml），不进 PR 路径。
  backend:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Detect backend changes
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        id: filter
        with:
          filters: |
            backend:
              - 'easyorange-backend/**'
      - name: Check cross-module optional deps (TD-011)
        # 除组合根 easyorange-application 外，模块依赖其他领域模块必须 <optional>true</optional>。
        if: steps.filter.outputs.backend == 'true' || github.event_name == 'workflow_dispatch'
        run: python3 .github/scripts/check-optional-deps.py easyorange-backend
      - name: Setup JDK 25
        if: steps.filter.outputs.backend == 'true' || github.event_name == 'workflow_dispatch'
        uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
      - name: Backend verify (11 modules, JaCoCo + Spotless gates)
        # -DskipITs=true：仅跳过 failsafe（*IT），IT 需要本地 docker compose dev 栈
        # （MySQL/Redis/RabbitMQ），CI runner 上没有。surefire 单测 + JaCoCo/Spotless 门禁照跑。
        if: steps.filter.outputs.backend == 'true' || github.event_name == 'workflow_dispatch'
        run: ./mvnw verify -B -Pci -DskipITs=true
        working-directory: easyorange-backend