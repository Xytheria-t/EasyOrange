<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.cartethyia</groupId>
        <artifactId>easyorange-backend</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>easyorange-application</artifactId>

    <properties>
        <!-- Flyway CLI 连接复用应用同一数据源约定：url/user 与 application.yaml 的 spring.datasource 缺省一致。
             密码不硬编码：与数据源共用环境变量 EASYORANGE_DB_PASSWORD（Spring Boot 启动已自动迁移，
             本 CLI 仅手动/CI 使用）。本地先 export EASYORANGE_DB_PASSWORD；生产/CI 用 -Dflyway.password 覆盖。 -->
        <flyway.url>jdbc:mysql://localhost:3306/easyorange?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true</flyway.url>
        <flyway.user>easyorange_app</flyway.user>
        <flyway.password>${env.EASYORANGE_DB_PASSWORD}</flyway.password>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-framework</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-common</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-user</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-product</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-favorite</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-order</artifactId>
        </dependency>

        <!-- 依赖支付模块（OrderPaymentGatewayAdapter 需要） -->
        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-payment</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-message</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-admin</artifactId>
        </dependency>

        <dependency>
            <groupId>com.cartethyia</groupId>
            <artifactId>easyorange-ai</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-elasticsearch</artifactId>
        </dependency>

        <!--
            链路追踪（OTel 桥）— micrometer Observation → OpenTelemetry SDK → OTLP 上报 Langfuse。
            替代 micrometer-tracing-bridge-brave：Brave 无 OTLP 出口（且 zipkin-reporter 依赖从未引入，
            原 Zipkin 上报实为死链路），OTel 桥是 Spring AI gen_ai 语义进 Langfuse 的唯一通路
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-opentelemetry</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-docker-compose</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ===== 集成测试基础设施（真实 MySQL/Redis/RabbitMQ）=====
             复用 Boot docker-compose：按仓库根 compose.yaml（start-only）保证 dev 栈运行，
             用例经 application-it.yaml 显式 localhost 直连。相比 Testcontainers 不依赖 ryuk sidecar，
             无代理 Docker 下不会被静默跳过。 -->

        <!-- Spring Modulith Event Publication Registry (JDBC backend, shares primary DataSource) -->
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-jdbc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                    <jvmArguments>
                        --sun-misc-unsafe-memory-access=allow
                        --enable-native-access=ALL-UNNAMED
                    </jvmArguments>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <version>${flyway.version}</version>
                <configuration>
                    <url>${flyway.url}</url>
                    <user>${flyway.user}</user>
                    <password>${flyway.password}</password>
                    <locations>
                        <location>filesystem:src/main/resources/db/migration</location>
                    </locations>
                    <placeholderReplacement>false</placeholderReplacement>
                    <cleanDisabled>true</cleanDisabled>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
