<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.cartethyia</groupId>
    <artifactId>easyorange-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <packaging>pom</packaging>

    <name>easyorange-backend</name>
    <description>easyorange-backend</description>

    <modules>
        <module>easyorange-common</module>
        <module>easyorange-framework</module>
        <module>easyorange-user</module>
        <module>easyorange-product</module>
        <module>easyorange-order</module>
        <module>easyorange-payment</module>
        <module>easyorange-message</module>
        <module>easyorange-favorite</module>
        <module>easyorange-admin</module>
        <module>easyorange-ai</module>
        <module>easyorange-application</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <lombok.version>1.18.46</lombok.version>

        <jjwt.version>0.13.0</jjwt.version>
        <aspectjweaver.version>1.9.25.1</aspectjweaver.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>

        <jackson3.version>3.2.1</jackson3.version>
        <jackson-annotations.version>2.22</jackson-annotations.version>
        <!-- Jackson 2.x 传递依赖固定到 2.22.1（含 CVE-2026-54515 修复，2.21.4 受影响） -->
        <jackson2.version>2.22.1</jackson2.version>
        <!-- 覆盖 Spring Boot 4.0.7 托管的版本：netty 追 CVE 补丁。Boot 后续补丁若托管更高版本可移除 -->
        <netty.version>4.2.17.Final</netty.version>
        <tomcat.version>11.0.24</tomcat.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok.mapstruct.binding.version>0.2.0</lombok.mapstruct.binding.version>
        <jetbrains.annotations.version>26.1.0</jetbrains.annotations.version>

        <!-- Flyway 数据库迁移（覆盖 Spring Boot 4.0.7 托管的 11.14.1，升至 13.2.0 当前稳定版；MySQL 8.4 支持完整） -->
        <flyway.version>13.2.0</flyway.version>
        <archunit.version>1.5.0</archunit.version>
        <redisson.version>4.7.0</redisson.version>
        <springdoc.version>3.1.0</springdoc.version>
        <swagger-annotations.version>2.2.53</swagger-annotations.version>
        <spring-ai.version>2.0.0</spring-ai.version>

        <!-- JaCoCo 覆盖率门禁：本地默认不阻断，CI 用 -Djacoco.haltOnFailure=true 启用 -->
        <jacoco.haltOnFailure>false</jacoco.haltOnFailure>

        <!-- PIT 变异测试（1.25.8 修复 Java 25 BigDecimal/BigInteger mutator） -->
        <pitest.version>1.25.9</pitest.version>
        <pitest-junit5.version>1.2.3</pitest-junit5.version>
        <!-- PIT 阈值门禁：本地默认 0（仅出报告不阻断），CI 用 -Dpit.mutationThreshold=60 等启用 -->
        <pit.mutationThreshold>0</pit.mutationThreshold>
        <pit.testStrengthThreshold>0</pit.testStrengthThreshold>
        <pit.coverageThreshold>0</pit.coverageThreshold>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-framework</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-user</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-product</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-order</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-payment</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-message</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-favorite</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-admin</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cartethyia</groupId>
                <artifactId>easyorange-ai</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

            <dependency>
                <groupId>org.aspectj</groupId>
                <artifactId>aspectjweaver</artifactId>
                <version>${aspectjweaver.version}</version>
            </dependency>

            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>3.20.0</version>
            </dependency>
            <dependency>
                <groupId>net.coobird</groupId>
                <artifactId>thumbnailator</artifactId>
                <version>0.4.21</version>
            </dependency>

            <dependency>
                <groupId>org.jetbrains</groupId>
                <artifactId>annotations</artifactId>
                <version>${jetbrains.annotations.version}</version>
            </dependency>

            <!-- 版本收敛：spring-ai 依赖 5.0.0，openai-java-core 传递依赖 4.38.0，显式钉到 5.0.0 消除冲突 -->
            <dependency>
                <groupId>com.github.victools</groupId>
                <artifactId>jsonschema-module-swagger-2</artifactId>
                <version>5.0.0</version>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok-mapstruct-binding</artifactId>
                <version>${lombok.mapstruct.binding.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- ==================== Jackson 3 当前稳定版（3.2.1） ==================== -->
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson3.version}</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson3.version}</version>
            </dependency>
            <!-- Jackson Annotations 2.22 (required by jackson-databind 3.x for @JsonSerializeAs) -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson-annotations.version}</version>
            </dependency>
            <!-- Jackson 2.x 传递依赖（第三方库引入）固定到 2.22.1，含 CVE-2026-54515 修复 -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson2.version}</version>
            </dependency>

            <!-- ==================== ArchUnit ==================== -->
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Spring Modulith -->
            <dependency>
                <groupId>org.springframework.modulith</groupId>
                <artifactId>spring-modulith-bom</artifactId>
                <version>2.1.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>


            <!-- Spring AI -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- OpenAPI 注解（@Tag/@Operation 等）— provided 作用域，仅编译期可见，运行时由 application 模块的 springdoc 依赖提供 -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
            <version>${swagger-annotations.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok.mapstruct.binding.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <!-- 编译期生成 spring-configuration-metadata.json，给 IDE 补全/提示配置键（零运行时开销） -->
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-Xlint:dangling-doc-comments</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.jvm=ALL-UNNAMED</arg>
                        <!-- Suppress: sun.misc.Unsafe::objectFieldOffset deprecated warning (JDK 24+, Lombok Permit class) -->
                        <arg>-J--sun-misc-unsafe-memory-access=allow</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- @{argLine} 注入 JaCoCo agent；后续 JVM 参数保障 WSL2 Mockito/反射可用 -->
                    <argLine>
                        @{argLine}
                        -Dfile.encoding=UTF-8
                        -Dsun.jnu.encoding=UTF-8
                        -Dstdout.encoding=UTF-8
                        -Dstderr.encoding=UTF-8
                        -Djdk.attach.allowAttachSelf=true
                        -Dnet.bytebuddy.experimental=true
                        -XX:+EnableDynamicAgentLoading
                        --sun-misc-unsafe-memory-access=allow
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens java.base/java.io=ALL-UNNAMED
                        --add-opens java.base/java.util=ALL-UNNAMED
                        --add-opens java.base/java.lang.invoke=ALL-UNNAMED
                        --add-opens java.base/java.text=ALL-UNNAMED
                        --add-opens java.base/java.nio=ALL-UNNAMED
                        --add-opens java.base/jdk.internal.misc=ALL-UNNAMED
                        --add-opens java.base/jdk.internal.reflect=ALL-UNNAMED
                        --add-opens java.base/sun.security.ssl=ALL-UNNAMED
                        --add-opens java.base/sun.nio.ch=ALL-UNNAMED
                        --add-opens java.base/sun.nio.fs=ALL-UNNAMED
                        --add-opens java.base/sun.security.action=ALL-UNNAMED
                        --add-exports java.base/sun.net.util=ALL-UNNAMED
                    </argLine>
                </configuration>
            </plugin>
            <!-- 集成测试（*IT）经 failsafe 绑定到 verify 阶段，与 surefire 单测（*Test）分离。
                 调用 `mvn verify` 时才会起真实基础设施（Boot docker-compose 复用 dev 栈），`mvn test` 保持快速单测。 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- 与 surefire 同一套 JVM 参数（含 JaCoCo agent @{argLine} 与 WSL2 Mockito/反射 flag） -->
                    <argLine>
                        @{argLine}
                        -Dfile.encoding=UTF-8
                        -Dsun.jnu.encoding=UTF-8
                        -Dstdout.encoding=UTF-8
                        -Dstderr.encoding=UTF-8
                        -Djdk.attach.allowAttachSelf=true
                        -Dnet.bytebuddy.experimental=true
                        -XX:+EnableDynamicAgentLoading
                        --sun-misc-unsafe-memory-access=allow
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens java.base/java.io=ALL-UNNAMED
                        --add-opens java.base/java.util=ALL-UNNAMED
                        --add-opens java.base/java.lang.invoke=ALL-UNNAMED
                        --add-opens java.base/java.text=ALL-UNNAMED
                        --add-opens java.base/java.nio=ALL-UNNAMED
                        --add-opens java.base/jdk.internal.misc=ALL-UNNAMED
                        --add-opens java.base/jdk.internal.reflect=ALL-UNNAMED
                        --add-opens java.base/sun.security.ssl=ALL-UNNAMED
                        --add-opens java.base/sun.nio.ch=ALL-UNNAMED
                        --add-opens java.base/sun.nio.fs=ALL-UNNAMED
                        --add-opens java.base/sun.security.action=ALL-UNNAMED
                        --add-exports java.base/sun.net.util=ALL-UNNAMED
                    </argLine>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <configuration>
                    <encoding>UTF-8</encoding>
                    <propertiesEncoding>UTF-8</propertiesEncoding>
                </configuration>
            </plugin>
            <!-- 构建环境硬约束：锁 Java 25 / Maven 3.9+ / 依赖收敛，违背即构建失败 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>enforce-requirements</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25,26)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9,)</version>
                                </requireMavenVersion>
                                <!-- 不启用 dependencyConvergence：本项目大量用 dependencyManagement 显式钉版
                                     （netty/tomcat/jackson/swagger/ai 覆盖），该规则会把「已显式收敛」的传递依赖
                                     也报成冲突，产生长尾噪声。版本收敛由显式 dependencyManagement +
                                     OWASP dependency-check（-Pci）+ Renovate 管线共同承担。 -->
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <configuration>
                    <haltOnFailure>${jacoco.haltOnFailure}</haltOnFailure>
                    <rules>
                        <rule>
                            <element>BUNDLE</element>
                            <limits>
                                <limit>
                                    <counter>LINE</counter>
                                    <value>COVEREDRATIO</value>
                                    <minimum>0.80</minimum>
                                </limit>
                                <limit>
                                    <counter>BRANCH</counter>
                                    <value>COVEREDRATIO</value>
                                    <minimum>0.60</minimum>
                                </limit>
                            </limits>
                        </rule>
                    </rules>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>

        <repository>
            <id>aliyunmaven</id>
            <name>Aliyun Maven</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>

        <repository>
            <id>tencentmaven</id>
            <name>Tencent Maven</name>
            <url>https://mirrors.cloud.tencent.com/nexus/repository/maven-public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>

    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>aliyunmaven</id>
            <name>Aliyun Maven</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </pluginRepository>
        <pluginRepository>
            <id>central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <profiles>
        <!--
            CI 门禁 profile — 本地默认关闭，仅 CI 用 `./mvnw -Pci` 启用：
              • Spotless 格式强制
              • JaCoCo 覆盖率门禁（check）：80% 行 / 60% 分支，低于阈值即失败
            本地 `./mvnw install` 会跳过（JaCoCo prepare-agent/report 仍在默认构建，
            本地 `./mvnw test` 照常收集覆盖率），构建速度显著提升。
            注：OWASP dependency-check 已移出本 profile —— 境外 runner 上 NVD 拉取动辄 30-40
            分钟，不适合放在主门禁里阻塞每次 push/PR。改由独立 `-Powasp` profile + 非阻断
            security job 承担（见 `-Powasp` 注释）。
        -->
        <profile>
            <id>ci</id>
            <build>
                <plugins>
                    <!-- 后端代码格式强制：palantir-java-format（4 空格 / 120 列，与 .editorconfig 一致），
                         进 verify 阶段 check，格式不达标即 CI 红。本地 `-Pci spotless:apply` 可一键规范化 -->
                    <plugin>
                        <groupId>com.diffplug.spotless</groupId>
                        <artifactId>spotless-maven-plugin</artifactId>
                        <version>3.9.0</version>
                        <configuration>
                            <java>
                                <palantirJavaFormat>
                                    <version>2.97.0</version>
                                    <style>PALANTIR</style>
                                </palantirJavaFormat>
                                <removeUnusedImports/>
                            </java>
                        </configuration>
                        <executions>
                            <execution>
                                <id>spotless-check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.8.14</version>
                        <executions>
                            <execution>
                                <id>coverage-check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!--
            OWASP dependency-check — 全依赖 NVD 漏洞扫描。独立 profile，由 CI 的 security job
            （非阻断，continue-on-error）单独启用，不混入主门禁 `-Pci`：
            境外 GitHub runner 上 nvd.nist.gov 慢/脆，首次扫描实测 39 分钟 —— 若放主门禁，
            每次 push/PR 都要等 30-40 分钟且随时可能因 NVD 抖动让 CI 红。
            本地按需运行：`./mvnw -Powasp verify -DskipTests`（需联网拉 NVD 增量，较慢）。
        -->
        <profile>
            <id>owasp</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>13.0.0</version>
                        <configuration>
                            <failBuildOnCVSS>8</failBuildOnCVSS>
                            <!-- 网络/基础设施故障（NVD 拉取失败、限流）不 fail —— 境外 runner 上
                                 nvd.nist.gov 慢/脆，缓存未命中时首次下载可能 30-40 分钟甚至超时。
                                 failOnError=false 让扫描失败只告警不 fail，真正的高危 CVE 仍由
                                 failBuildOnCVSS=8 兜底标记。即"实漏洞才红，基础设施抖动不红" -->
                            <failOnError>false</failOnError>
                            <formats>
                                <format>HTML</format>
                            </formats>
                            <skipTestScope>true</skipTestScope>
                            <skipProvidedScope>true</skipProvidedScope>
                            <!-- 显式数据目录：CI 里用 actions/cache 缓存它，NVD 库跨 run 复用，
                                 避免每次全新下载整库（nvd.nist.gov 慢/限流会让首次 run 挂 30+ 分钟） -->
                            <dataDirectory>${user.home}/.dependency-check-data</dataDirectory>
                            <!-- 关闭 OSS Index 第三方补充源：本环境网络不可达（每次查询都失败产生告警），
                                 权威 NVD 源足以支撑扫描；保留其余 analyzer（NVD/GitHub Advisory） -->
                            <ossindexAnalyzerEnabled>false</ossindexAnalyzerEnabled>
                            <!-- 关闭 RetireJS：其 jsrepository.json 托管于 GitHub raw，本环境网络不可达，
                                 每次扫描都 Read timed out 中止整体分析。本项目为纯 Java 后端，唯一 JS 依赖
                                 （swagger-ui 捆绑 DOMPurify）已在 dependency-suppression.xml 定向豁免 -->
                            <retireJsAnalyzerEnabled>false</retireJsAnalyzerEnabled>
                            <!-- 关闭 .NET Assembly Analyzer：本环境未安装 dotnet，每次扫描 Jar 都尝试
                                 分析 .NET 程序集并报 "dotnet executable not found" 错误，纯噪声无价值 -->
                            <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
                            <!-- 定向豁免无修复版/误报的 CVE（kotlin-stdlib、mysql-connector-j），见 dependency-suppression.xml -->
                            <suppressionFiles>
                                <suppressionFile>${maven.multiModuleProjectDirectory}/owasp/dependency-suppression.xml</suppressionFile>
                            </suppressionFiles>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!--
            PIT 变异测试 — 评估测试套件对缺陷的真实检测能力（行/分支覆盖率的"金标准"补充）。
            默认不启用：PIT 较慢，按需运行 `./mvnw -Ppit test-compile pitest:mutationCoverage`。
            仅对 domain 层注入变异（聚合根状态机 / 领域服务 / 值对象校验），聚焦业务逻辑测试质量，
            避开 adapter / config / DTO 等样板代码的噪声变异。
            HTML 报告：target/pit-reports/index.html

            阈值门禁（对齐 JaCoCo haltOnFailure 约定）：本地默认 0 = 仅出报告不阻断；
            CI 启用门禁示例（基于 order 模块基线 70% mutation / 89% strength / 81% line）：
              ./mvnw -Ppit test-compile pitest:mutationCoverage \
                -Dpit.mutationThreshold=60 -Dpit.testStrengthThreshold=75 -Dpit.coverageThreshold=70
        -->
        <profile>
            <id>pit</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>${pitest.version}</version>
                        <dependencies>
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>${pitest-junit5.version}</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <!-- 仅对 domain 层变异：聚合根状态机、领域服务、值对象校验 -->
                            <targetClasses>
                                <param>com.cartethyia.easyorange.*.domain.*</param>
                            </targetClasses>
                            <!-- 运行所有可能覆盖被变异代码的测试（PIT 按行覆盖自动筛选） -->
                            <targetTests>
                                <param>com.cartethyia.easyorange.*</param>
                            </targetTests>
                            <mutators>
                                <mutator>DEFAULTS</mutator>
                            </mutators>
                            <outputFormats>
                                <outputFormat>HTML</outputFormat>
                                <outputFormat>XML</outputFormat>
                            </outputFormats>
                            <!-- 覆盖式报告，便于 CI 解析与本地查阅 -->
                            <timestampedReports>false</timestampedReports>
                            <!-- 模块无 domain 类时不报错（如纯 boot 模块） -->
                            <failWhenNoMutations>false</failWhenNoMutations>
                            <!-- 阈值门禁：默认 0 = 不阻断；CI 用 -D 覆盖启用 -->
                            <mutationThreshold>${pit.mutationThreshold}</mutationThreshold>
                            <testStrengthThreshold>${pit.testStrengthThreshold}</testStrengthThreshold>
                            <coverageThreshold>${pit.coverageThreshold}</coverageThreshold>
                            <!-- 注意：不配置 historyInputFile/historyOutputFile —— 增量历史是 PIT 的付费
                                 功能（pitest-history 商业插件，arcmutate），免费版开启会直接报
                                 "History has been enabled but no history plugin has been installed" 构建失败 -->
                            <jvmArgs>
                                <jvmArg>-XX:+EnableDynamicAgentLoading</jvmArg>
                                <jvmArg>--sun-misc-unsafe-memory-access=allow</jvmArg>
                                <jvmArg>--add-opens java.base/java.lang=ALL-UNNAMED</jvmArg>
                                <jvmArg>--add-opens java.base/java.lang.reflect=ALL-UNNAMED</jvmArg>
                                <jvmArg>--add-opens java.base/java.util=ALL-UNNAMED</jvmArg>
                            </jvmArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
