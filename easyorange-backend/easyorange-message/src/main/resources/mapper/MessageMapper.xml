<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cartethyia.easyorange.message.adapter.outbound.persistence.MessageMapper">

    <!-- 会话列表：库端聚合每个会话对方的最新一条消息（UUIDv7 时间有序，MAX(id) 即最新），替代全量拉取内存聚合 -->
    <select id="selectLatestPerConversation" resultType="com.cartethyia.easyorange.message.adapter.outbound.persistence.MessageDO">
        SELECT m.id, m.sender_id, m.receiver_id, m.type, m.title, m.content,
               m.is_read, m.read_time, m.business_id, m.conversation_id,
               m.del_flag, m.create_time, m.update_time, m.create_by, m.update_by, m.version
        FROM eo_message m
        JOIN (
            SELECT MAX(id) AS max_id
            FROM eo_message
            WHERE (sender_id = #{userId} OR receiver_id = #{userId}) AND del_flag = 0
            GROUP BY CASE WHEN sender_id IS NULL THEN 'system'
                          WHEN sender_id = #{userId} THEN receiver_id
                          ELSE sender_id END
        ) t ON m.id = t.max_id
        ORDER BY m.create_time DESC
    </select>

    <!-- 会话列表：按会话对方聚合的未读数（仅统计接收方向） -->
    <select id="countUnreadPerConversation" resultType="map">
        SELECT CASE WHEN sender_id IS NULL THEN 'system'
                    WHEN sender_id = #{userId} THEN receiver_id
                    ELSE sender_id END AS other_id,
               COUNT(*) AS cnt
        FROM eo_message
        WHERE receiver_id = #{userId} AND is_read = #{unreadCode} AND del_flag = 0
        GROUP BY other_id
    </select>

</mapper>
