# 面试脚本 — 先读这页

> **定位口径**：EasyOrange = **Java AI Agent 工程化实战**（生产级单 Agent 工程化）。主线是 AI Agent，DDD 与分布式可靠性是**工程底座**（工程素养证据），不是并列主线——投递话术不走架构师方向。
>
> **数字单一来源**：[doc/工程指标.md](../工程指标.md)——各册只引用不另设口径；**结构计数**（模块 / Port / ADR / 消费者 / 表 / ArchUnit 规则 / Prompt 模板 / 前端测试文件 / 金标准集条数）只在[「结构计数」](../工程指标.md#结构计数)区块维护，各册一律不复制数字。

## 一、册序（按使用时机排，不是按主题）

| 册 | 什么时候翻开 | 内容 |
|---|---|---|
| **01** [备战清单](01-备战清单.md) | **每天从这里进** | bullet × 出处 × 状态 · 破防台账 · P1~P2 周排期 |
| **02** [代码走读](02-代码走读.md) | 学习期：读代码 | AI 侧 9 章 + 底座 8 章，点名到类 |
| **03** [模拟题库](03-模拟题库.md) | 学习期：被拷打 | 追问链（AI 主线 / 底座）+ 行为面 H1~H8 |
| **04** [速答](04-速答.md) | 学习期：崩了来补漏 | 高频技术题 11 组，带项目结合 |
| **05** [设计题与手撕](05-设计题与手撕.md) | 学习期：练手撕 | 设计题 7 道 + 手撕 10 题 |
| **06** [工程底座](06-工程底座.md) | 被追问架构时 | DDD 核心思想 +「刻意不做」取舍话术 |
| **07** [怎么说](07-怎么说.md) | 投递与面试 | 岗位矩阵 / 简历 / 自我介绍 / 追问应答 / 盲区 / 数字锚点 / 话术速查 |

**学习期的日常顺序是 01 → 02 → 03，崩了回 04 / 05 补**；06 只在被追问架构时翻。**07 到投递期（P3）才需要**——W1 就读它只会增加焦虑，不产生能讲的内容。

## 二、引用约定

- 册间按节号引用：`02 §5` = [代码走读](02-代码走读.md) 第 5 节、`05 题 1` = [设计题与手撕](05-设计题与手撕.md) 第 1 题
- [速答](04-速答.md) 按组-题号引用：`04 AI-1` / `04 RAG-4`（组内编号稳定，不随插入漂移）
- 阶段 / 周次 / 投递时间线口径见 [迭代路线](../迭代路线.md)

## 三、学习协议

### 三个开场（AI 陪练会话用）

| 开场白 | 前置 | AI 行为 |
|---|---|---|
| 「开始第 N 章」 | — | 给读码路径卡：入口文件 + 阅读顺序 + 自检问题清单，不给讲解 |
| 「拷打第 N 章」 | **该章白话讲解已归档**；没归档就退回上一条，不拷打 | 当面试官连环追问两轮：模块内 + 跨链路串联；崩的点按三档记入破防清单 |
| 「继续破防清单」 | 清单里至少有一条 A/B 档 | 抽样考：A 档全考 + B 档抽 3 + 当周新学 2；答对一轮的状态当场改，不留到下一轮 |

> **别在没读过时拷打**。拷打的产出是「学过但崩了」的残留，不是诊断工具；手上还没有存量时去拷打，结果只会是「整章都是破防点」——那是课表，不是清单。

### 每章固定循环

1. 拿路径卡 → 自读代码（AI 不讲解；卡住超 30 分钟才解锁讲解，讲完须复述回来）
2. 合上文档写 ≤10 行白话讲解（写不出 = 没读完，回去重读）
3. 链路内拷打 → 跨链路串联拷打 → 崩的点按档进破防清单（自读与白话讲解没做完的章不拷打）
4. A/B 档清零 + 该章挂的 bullet 勾选判据全过 = 出关；出关产出白话讲解并入对应册（速答 → 04 / 走读 → 02 / 设计题 → 05）

### 队列（严格顺序，AI 主线在前）

| 章 | 主题 | 出关实验 |
|---|---|---|
| ① | 自治式工具循环（决策 → 工具 → 观察 / 步数上限 / 三口径降级 / 步级 trace / SSE step 事件） | 本地打流式口跑量，看 `eo_agent_step_trace` 与 `easyorange.ai.chat.loop{outcome}` |
| ② | 多范式编排对照（Workflow 式 4 路扇出 vs 自治循环；循环控制权为什么不托管给框架） | 对比 `AiSearchEnhancerAdapter` 与 `AgentLoopRunner` 两处编排的控制流 |
| ③ | RAG 完整链路（摄入 / 分块 / 两路独立召回 / RRF 融合 / 引用溯源 / 三条链路共用同一份融合） | 手改 query 观察两路召回各自排名与融合后排名 |
| ④ | 评估闭环（金标准集 / Judge 对照 / EvalGate 双门禁 / 反馈飞轮 / 检索指标） | 改一版 prompt 后 dispatch `ai-eval.yml` 看门禁是否卡住 |
| ⑤ | 成本与延迟治理（限流 / TokenBudget / 语义缓存 / 模型路由 / 上下文裁剪 / 流式） | 读 `GET /api/admin/ai/cost-report` 与预算场景配置 |
| ⑥ | MCP 工具面与信任边界（两级暴露 / 公开只读面 / 独立限流） | Cursor 实连 `/mcp`，走通 search_products |
| ⑦ | LLM 可观测（OTel 桥 → Langfuse / per-step prompt·token·延迟·成本 / 与 Grafana 分工） | 起 langfuse profile，看单条 trace 内的步级 GENERATION |
| ⑧ | 工程底座（下单一致性 / Outbox + DLQ / 缓存三防 / ArchUnit + ADR + PIT） | 跑 `./mvnw test` 看架构守卫红闸门 |
| ⑨ | 取舍话术与盲区绕法（纯背 + 抽查） | 对着 [07 §6](07-怎么说.md) 逐个自问 |

- 压测 / 评测 / 跑量类实验跟**数字补测**走：每补一个数字当场落 [工程指标.md](../工程指标.md)，再回填 [07 §9](07-怎么说.md) 数字锚点
- 算法刷题每日自理，不在队列内

### 破防清单（规则）

**台账单一来源**：[01 §7](01-备战清单.md)。本页只定规则，不另存一份——两处台账不互通，等于没有进度。

**准入**：只记「自读 + 合书白话讲解」做完后仍然崩的点。还没读到那章的答不出，是**未学**不是崩，不入清单；否则清单第一天就等于整本课表。

**分档**（按面试 ROI，同 [AGENTS.md 功能取舍判据](../../AGENTS.md)）：

| 档 | 判据 | 处理 |
|---|---|---|
| **A 挡门** | 这条崩，整条 bullet 讲不了 | 本轮必须清，不许过夜 |
| **B 追问层** | 第 2 层追问崩 | 进清单，按周清 |
| **C 细节** | 第 3 层或纯细节 | **允许认赔**：记「放弃 + 理由」后从清单划掉，理由进 [07 §6](07-怎么说.md) 当绕法素材 |

**状态机**：`崩 → 已答对一轮 → 清零`（连续两轮答对）。答对一轮就当场改状态，别等清零才动——中间态是唯一能看见进度的地方。

**WIP 上限**：未清零的 A+B 档合计 **≤ 12 条**。满了**不许开新章**，先清债；清债速度追上新增速度之前，清单只会膨胀成第二本课表。

**出关**：该章挂的 bullet 勾选判据全过（判据见 [01 §1](01-备战清单.md)）。
