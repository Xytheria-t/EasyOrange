# 架构参考（后端原则 / 模块依赖 / 错误码 / 已知问题）

> 原 AGENTS.md 章节，按需读取，不常驻上下文。改架构、跨模块、异常/错误码、排查已知坑时先读本文件。

## 后端架构核心原则

1. **DDD 分层**: domain → application → adapter，依赖方向单向向内
2. **CQRS**: 命令与查询分离 (product, order, payment, message 模块)
3. **六边形架构**: domain 层通过 port 接口与外部解耦
4. **不可变性**: 聚合根用 `@Builder(toBuilder = true)`，值对象用 `record`
5. **领域事件**: `DomainEventPublisher` 发布事件 → `ModulithDomainEventPublisher`（`@Primary`）代理到 `ApplicationEventPublisher` → Spring Modulith 在数据库 `EVENT_PUBLICATION` 表中持久化事件（与应用事务同原子） → 异步从 `EVENT_PUBLICATION` 读取并发布到 **RabbitMQ Topic Exchange** (`eo.domain.events`)。路由键由事件类名自动派生（`ProductCreatedEvent` → `product.created`），无需手动注册。每个消费者独占队列（`eo.{name}`），失败消息路由到 DLQ（`eo.{name}.dlq`）→ `DlqRetryScheduler` 每 5 分钟扫描 DLQ，按 1/5/15min 指数退避重投主队列（重试次数 < 3），超过 3 次的毒消息转储 `eo.dlq.terminal` 等待人工介入。Modulith 的 at-least-once 语义 + 消费者 `EventIdempotencyChecker` 确保精确一次处理。审计日志同样走 Outbox 模式（`AuditLogAspect` 发布 `AuditLogEvent` → `AuditLogEventConsumer` 异步入库）。采用 RabbitMQ-only 模式（`@ConditionalOnProperty(matchIfMissing=true)` 保留以防无 RabbitMQ 环境）。**traceId 全链路传递**：Brave `TracingFilter` 从 HTTP 头提取 traceId → 写入 MDC → `MdcTaskDecorator` 传递到 @Async 虚拟线程 → `EventMetadataMessagePostProcessor` 发布前把 traceId 注入 RabbitMQ message header → 消费者端 `EventMetadata.from(message, event)` decode 回 MDC → 日志 + Micrometer metrics 统一关联。**已实现 12 个事件消费者**: ProductEventConsumer (内部 CQRS 投影), AiProductEventConsumer (AI 估值/文案), OrderNotificationEventConsumer (站内信), OrderLifecycleEventConsumer (订单生命周期: 取消/退款恢复库存, 完成标记售出), PaymentSucceededEventConsumer (支付成功→订单置 PAID 桥接), AiCreditEventConsumer (信用分), ProductAuditEventConsumer (审核通知), ReportProcessedEventConsumer (举报通知), WebSocketEventConsumer (消息撤回广播), PaymentMetricsConsumer (支付指标), FavoritePriceDropEventConsumer (收藏降价提醒), **AuditLogEventConsumer (审计日志 Outbox 入库)**
6. **Assembler 模式**: DTO 转换统一在 `adapter/inbound/web/assembler/` 目录下实现（FavoriteAssembler, CategoryAssembler, PaymentViewAssembler, UserAssembler）。**禁止**在 Controller/Service 中直接构造 Response DTO。已废弃旧 DTO（AddFavoriteDTO, FavoriteVO, PaymentQuery, PaymentView, PaymentMethodVO 等；`QueryOrderRequest` 仍为现役请求 DTO，Controller 负责转为 `OrderListQuery`）
7. **ACL 隔离**: 跨模块通过 ACL/Port 适配，禁止直接依赖领域模型
8. **异常继承**: 领域异常必须继承 `BaseBusinessException`（common 模块），实现 Spring `ErrorResponse`（`getStatusCode()` 委托 `IResultCode.resolveStatus(code)`，映射单一来源在错误码枚举）。`GlobalExceptionHandler` 使用 Java 21 模式匹配 switch 在单个 `handle()` 方法内按类型分发，业务异常状态码直接取自 `BaseBusinessException#getStatusCode()`（按错误码自动映射：A 段取码内数字推导 4xx，如 A0401/A04011→401/A0403→403/A0404→404/A0405→405/A0413→413/A0429→429，其余 A→400；B→400/C→500/D→502）+ 业务错误码；校验类错误统一返回 400。**禁止直接抛出非 `BaseBusinessException` 子类的 RuntimeException**，否则会落入 500 兜底（IllegalArgumentException 等编程错误一律 500，不做客户端美化）。`BusinessException` 和 `FileException` 构造器均设为 `protected`，抛业务异常时统一使用 `BusinessException.of(...)` / `FileException.of(...)` 工厂方法；子类可正常调用 `super(...)`。各模块领域异常必须使用模块专属 `ResultCode`（如 `ProductResultCode.PRODUCT_NOT_FOUND`），**禁止回退到全局 `B0002`**
9. **方法参数级校验**: 控制器**不要**标 `@Validated` —— 类级 `@Validated` 会让 Spring MVC 内建方法校验失效（`HandlerMethod$MethodValidationInitializer` 见 `@Validated` 即跳过），改由 AOP 抛 `ConstraintViolationException`；两条路径（内建 → `HandlerMethodValidationException` / AOP → `ConstraintViolationException`）`GlobalExceptionHandler` 都已映射为 400 + `B0003`，但内建路径是 Spring 6.1+ 的推荐做法，且不产生控制器 AOP 代理
10. **持久化异常在适配器内翻译**: `org.springframework.dao` 家族是持久化技术细节，**不得渗进 `domain`/`application`**（ArchUnit 守卫：`domain`/`application` 禁依赖 `org.springframework.dao`）。唯一键冲突等预期内竞争在适配器里翻译成端口语义（如 `FavoriteRepository#saveIfAbsent` 返回 `Optional.empty()` 表示"已存在"，幂等成功）；`ConcurrentUpdateException`（乐观锁 0 行命中）、`CannotAcquireLockException`（MySQL 死锁 1213 / 锁等待超时 1205 → 400 + `B0006`）由适配器或全局兜底映射，业务层不 catch 持久层异常

## 模块依赖关系

```
application → framework, common, user, product, order, payment, message, favorite
framework → common
user → framework, common
product → framework, common, user (通过 SellerInfoPort 隔离，optional)
order → framework, common, product, user, payment (通过 Port 接口隔离，optional)
payment → framework, common
message → framework, common, user (通过 UserInfoPort 隔离，optional)
favorite → framework, common, product (通过 ProductInfoPort / PriceDropNotificationPort 隔离，optional)
ai → framework, common, product (通过 ProductSearchQueryPort 隔离)
admin → framework, common（其余模块零依赖，全部通过 8 个 Admin*Port 隔离（分类/用户/商品/订单/审核/评价/举报/仪表板），2026-08-08 收口）
```

> **状态**：所有跨模块依赖已通过端口接口 + 适配器模式隔离，Maven 依赖标记为 `<optional>true</optional>`。写操作通过事件驱动解耦，查询操作保留同步端口调用。
>
> **演进说明**：`BaseDO`、`BaseRepository`、`IdGenerator`、`ConcurrentUpdateException` 已从 `framework` 下沉至 `common` —— 业务模块依赖 `framework` 是基础设施层面的模块聚合（每个模块的 `domain/` 包不导入 framework 代码），实际 DDD 层约束由 ArchUnit 在包级别守卫。

## 错误码规范

错误码采用 **A/B/C/D 前缀格式**：

| 前缀 | 类型 | HTTP 状态 | 示例 |
|------|------|----------|------|
| A | 成功/客户端语义 | 200/4xx | A0000=成功, A0401=未登录, A04011=登录已过期, A0403=禁止访问, A0413=上传文件过大, A0429=请求过于频繁（限流） |
| B | 业务错误 | 400 | B0001=操作失败, B0002=业务异常 |
| C | 系统错误 | 500 | C0500=服务器内部错误 |
| D | 第三方错误 | 502 | D0502=上游服务不可用（支付网关适配器 `PaymentGatewayAdapterException`） |

判断成功：`"A0000".equals(code)`

**A 段编号布局**：A + 4 位 HTTP 状态码 + 可选子码数字。A0000 成功；0401=401 未登录 / 04011=401 子码·登录已过期（0402 为 HTTP 402 语义，子码不占用真实 4xx；由 `SecurityConfig#resolveAuthFailureCode` 按 JWT 过期与否区分 A0401/A04011）/ 0403=403 / 0404=404 / 0405=405 / 0413=413 上传文件过大（`spring.servlet.multipart.max-file-size` 超限，dev 10MB / prod 5MB）/ 0429=429。**HTTP 状态由码内数字自动推导**（`IResultCode#resolveStatus` 取 `code[1,5)` 解析，仅接受合法 4xx，否则 400），新增 A04xx 家族码无需改映射。

**B 段通用码语义**：B0001=通用失败（`Result.error(String)` 兜底）、B0002=未指定码的业务异常兜底、B0003=参数校验失败（@Valid/约束/参数缺失/解析错误）、B0004=请求形态错误（如 415 媒体类型）、B0005 预留。

HTTP 状态映射单一来源：`IResultCode#resolveStatus(String)` / `#httpStatus()`（common 模块），`GlobalExceptionHandler` 与 `BaseBusinessException#getStatusCode()` 均消费此映射。

### 错误码模块分配表

B 前缀（业务错误码）按模块分段，新增模块时在预留段内分配：

| 范围 | 模块 | 实现类 |
|------|------|--------|
| B000x | 全局通用 | `ResultCode` |
| B1xxx | user | `UserResultCode` |
| B2xxx | product | `ProductResultCode` |
| B3xxx | order | `OrderResultCode` |
| B4xxx | payment | `PaymentResultCode` |
| B5xxx | file | `FileResultCode` |
| B6xxx | admin | `AdminResultCode` |
| B7xxx | message | `MessageResultCode` |

> 新增模块时优先使用 B8xxx-B9xxx 预留范围，避免与现有范围冲突。各枚举统一使用 `@Getter @AllArgsConstructor` 模式实现 `IResultCode`。

## 已知问题

- **OWASP 硬闸门已恢复绿色（2026-08-04）**: 通过依赖升级 + 定向豁免清除全部 CVSS ≥ 8 告警：
  - Spring Boot 4.0.3 → **4.0.7**（spring-framework 7.0.8，修复 **CVE-2026-41855** 9.8）；netty → **4.2.16**、tomcat → **11.0.24**、jackson → **3.1.5**（见 pom `<netty.version>`/`<tomcat.version>`/`<jackson3.version>` 覆盖）
  - **豁免项**（`easyorange-backend/owasp/dependency-suppression.xml`，注明理由、待条件满足后解除）：kotlin-stdlib **CVE-2026-53914**（修复版 Kotlin 2.4.20 未发布稳定版；纯 Java 项目不编译 Kotlin，攻击面≈0）；mysql-connector-j **CVE-2026-60193/60192**（受影响组件为 Connector/Net(.NET)，非 Connector/J(Java JDBC)，且 9.7.0 已是最新版）
  - **环境提示**：OWASP 依赖检查已从 `-Pci` 主门禁拆出，改为独立 `-Powasp` profile + nightly schedule workflow（`.github/workflows/security-scan.yml`，每日 02:00 UTC + 手动触发；NVD 在境外 runner 上首次扫描实测 39 分钟，不适合阻塞每次 push/PR；`failOnError=false` 使 NVD 抖动只告警不红，真实 CVSS ≥ 8 仍会标红）。本地默认构建完全跳过 OWASP（提速显著）；按需扫描用 `./mvnw -Powasp verify -DskipTests`。检查需联网拉取数据源（OSS Index API 需 token、RetireJS 从 raw.githubusercontent 下载），本机若遇 401/超时可用 `-Danalyzer.ossindex.enabled=false` + `-Danalyzer.retirejs.enabled=false`，或临时 `-Ddependency-check.skip=true`
- **Redis 连接**: `REDIS_PASSWORD` 的 dev 默认值 `easyorange123` 三处对齐——`application-dev.yaml` 的占位符默认值、`.env.example` 模板、`compose.yaml` 的 `${REDIS_PASSWORD:-easyorange123}`。`application.yaml` 的 base 只写 `${REDIS_PASSWORD}`（无默认值），由 dev profile 兜底，避免 prod 拿到弱默认密码。若仍遇到 `Unable to connect to Redis` 错误，检查：① 是否已执行 `docker compose up -d` 启动 Redis；② 环境变量 `REDIS_PASSWORD` 是否被设置为空值覆盖了默认值；③ **YAML 占位符必须用 `${VAR:default}`（单冒号），不要用 bash 风格的 `${VAR:-default}`**（多一个 `-`）—— Spring 会把 `-default` 当字面量默认值，导致 Lettuce 实际发出去的密码比预期多一个前导连字符，触发 `WRONGPASS invalid username-password pair`。**注意**：`compose.yaml` 和 shell 用 `:-` 是正确的（Docker Compose / bash 语法），但所有 `application*.yaml` 必须用单冒号。新增/修改 Spring Boot 配置占位符时，复制粘贴前先确认语法
