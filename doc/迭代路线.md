# 迭代路线 — Agent 工程化升级（2026 Q4）

> **用途**：EasyOrange 的**剩余事项**与验收口径；依据是 2026-09-19 的定位共识（面试 ROI 判据，见 [AGENTS.md](../AGENTS.md) 定位段与「双项目分工」条）。
> **已落地项不留档**（多步工具循环 / MCP server / Langfuse / 面试脚本收口）：现状见 [README](../README.md) 与 [工程指标 §1.3](工程指标.md)（含各项 `待补口径` 与复现命令）。
> **数字纪律**：每步先更新 [工程指标.md](工程指标.md)，再回填简历与面试脚本——顺序不能反。

## 双项目排期

| 时段 | 内容 |
|---|---|
| 2026-09 ~ 11 | EasyOrange sprint（每周 ≥10h） |
| 2026-12 | 第二项目启动（Python LangGraph 多智能体编排 + 评估，经 MCP 消费本仓库 Java 工具面） |
| 2027-02 | 第二项目 MVP |
| 2027 春 | 简历双项目就位，投日常实习 |

**分工边界**（写进两边 README / 简历，防关键词互抢）：EasyOrange = RAG + 单 Agent 深挖 + MCP server + 评估闭环 + 成本/降级治理（Java 工程化叙事）；第二项目 = LangGraph 多智能体 + 差异化场景（Python 为主体，经 MCP 消费 EasyOrange 工具面组成跨语言系统），**不做知识库问答**。

**上下文 / 记忆归属**（防关键词互抢的细化）：单循环内的 token 预算与裁剪、用户画像注入 prompt = **EasyOrange**（生产级单 agent 的上下文治理）；跨节点状态传递 / checkpointer / 子图上下文隔离、跨会话记忆库（LangGraph Store 系）= **第二项目**。一句话口径：EasyOrange 回答「一个 agent 怎么做到生产级」，第二项目回答「多个 agent 怎么编排与评估」。

## 剩余事项

- **数字补测**（口径与复现命令见 [工程指标 §二](工程指标.md)）：循环三口径 / 上下文注入 token / 检索指标 / RAG 有效性对照 / 压测——**靠真实 key 的项按 AGENTS.md 规则标记后搁置**，不阻塞其它工作
- **演示素材**：3 条 GIF（多步 Agent 找货流式 / Cursor 连 MCP 实时查在售商品 / Langfuse trace 面板）落 `doc/assets/gifs/`，README 的演示注释块随录制解开
- **评估**：金标准集扩 agent 场景用例；改 prompt / 换模型 / 调检索参数后手动 dispatch `ai-eval.yml`（历史 Judge 数字不可引用）
- **简历**：Windows 简历壳回填新 bullet → 重渲染成品 HTML/PDF（`print-preview-qa.py`）
- **学习清单**（出关机制见 [00-怎么说.md §〇](interview/00-怎么说.md)）：ReAct 论文口径 +「工具循环 vs Function Calling」差异应答 · MCP 协议（JSON-RPC / 传输 / tool schema）· LangGraph 与自研循环的对比应答 · RAG 评估指标与 RAGAS 口径 · 多智能体范式（第二项目预习）

## 收口纪律

见 [AGENTS.md](../AGENTS.md)「提交规范」与「开发中增量验证」；每步：涉及模块测试全绿 + ArchUnit 不破 + 数字先落工程指标。
