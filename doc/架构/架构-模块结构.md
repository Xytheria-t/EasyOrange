# EasyOrange 模块结构

> 本文档定义了 EasyOrange 项目各模块的详细包结构和分层规范。

---

## 一、核心公共模块

### 1. easyorange-common（通用基础模块）

包含通用工具、统一响应、基础异常、领域事件接口等所有模块共享的基础定义：

```
easyorange-common
├── com.cartethyia.easyorange.common
│   ├── annotation            # 通用注解 `[现状]`
│   │   ├── SkipRateLimit.java    # 跳过限流标记
│   │   └── SkipRepeatSubmit.java # 跳过防重标记
│   │   > 注：限流/防重已改为 Filter 配置驱动，操作日志为约定式自动记录
│   ├── constant              # 通用常量
│   │   └── CommonConstant.java
│   ├── dto                   # 通用 DTO
│   │   └── PageRequest.java  # 分页请求
│   ├── security              # 安全认证模型
│   │   └── AuthUser.java     # 认证用户信息 (Security Principal)
│   ├── enums                 # 通用枚举
│   │   ├── IResultCode.java  # 结果码接口
│   │   ├── ResultCode.java   # 通用结果码
│   │   ├── BusinessType.java # 业务操作类型
│   │   └── FileResultCode.java # 文件处理结果码
│   ├── event                 # 领域事件基础设施
│   │   ├── DomainEvent.java          # 领域事件接口（事件 record 实现此接口）
│   │   ├── DomainEventPublisher.java # 事件发布者接口
│   │   └── DomainEventSubscriber.java # 事件订阅者接口
│   ├── exception             # 基础异常
│   │   ├── BaseBusinessException.java       # 业务异常基类（含 code）
│   │   ├── BusinessException.java           # 通用业务异常
│   │   ├── file/
│   │   │   ├── FileException.java               # 文件异常
│   │   │   ├── FileSizeLimitExceededException.java
│   │   │   └── InvalidExtensionException.java
│   │   └── validation/
│   │       └── ParamValidationException.java     # 参数校验异常
│   ├── result                # 统一响应体
│   │   ├── Result.java       # 统一响应体 R<T>
│   │   └── PageResult.java   # 分页结果（纯 Java record，无框架依赖）
│   └── util                  # 通用工具类
│       ├── BizRequire.java   # 业务断言工具
│       ├── FileSizeFormat.java # 文件大小格式化工具
│       ├── MaskUtils.java    # 脱敏工具
│       ├── UuidV7IdGenerator.java # UUID v7 (RFC 9562) 主 ID 生成器
│       └── UuidV7IdGenerator.java   # UUID v7 主实现（RFC 9562）
```

> **现状**：`BaseDO`、`RequestUtil`、`FileUtils` 已迁移到 framework 模块。common 模块当前仍依赖 `mybatis-plus-extension`（用于 `PageRequest` 分页支持）和 `spring-web`（用于 `HttpStatus` 枚举），并非完全无框架依赖。

#### common 与 framework 的边界

| 维度 | common | framework |
|------|--------|-----------|
| 依赖范围 | JDK + Jackson + Spring Core/Web + MyBatis-Plus Extension + SLF4J + Jakarta Validation | 依赖 Spring 完整生态/Redis/MQ/MyBatis-Plus Starter 等框架库 |
| 内容类型 | 基础定义、工具类、异常定义、枚举 | 配置类、过滤器、AOP 切面、安全拦截、服务实现 |
| 典型类 | `Result`、`BizRequire`、`DomainEvent`、`FileSizeFormat` | `RedisConfig`、`SecurityConfig`、`BaseDO`、`RequestUtil` |
| 是否启动 Spring 上下文 | 否（纯定义和工具） | 是（含 `@Configuration`、`@Service` 等） |

**迁移规则**：如果一个类正常运行需要 Spring 容器初始化（如注入 Bean、使用 `@Configuration`），则必须放在 `easyorange-framework`。

> **已知问题** `[演进]`：`PageRequest` 依赖了 `mybatis-plus-extension` 的分页类，导致 common 模块引入了 MyBatis-Plus 依赖。应将 `PageRequest` 迁移到 framework 模块，或在 common 中定义纯 JDK 分页模型，由 framework 层适配 MyBatis-Plus 分页。

---

### 2. easyorange-framework（框架适配层）

放和 Spring / 中间件相关的通用封装，和 common 区分开：

```
easyorange-framework
├── com.cartethyia.easyorange.framework
│   ├── audit/                # 审计日志（Outbox 模式：Aspect 发布事件 → Consumer 异步入库）
│   │   ├── aspect/AuditLogAspect.java
│   │   ├── entity/AuditLog.java
│   │   ├── event/            # AuditLogEvent + AuditLogEventConsumer
│   │   ├── job/AuditLogCleanupTask.java
│   │   ├── mapper/AuditLogMapper.java
│   │   └── service/AuditLogService.java
│   ├── auth/                 # 认证（JWT Access + Opaque Refresh）：TokenService / TokenRotation / RefreshTokenStore
│   ├── config/               # 框架配置
│   │   ├── async/            # ThreadPoolConfig + MdcTaskDecorator + LoggingRejectedExecutionHandler
│   │   ├── cache/RedisCacheConfig.java  # Spring Cache 注解式 + Redis 单层（2026-08-13 替代手写多级缓存，fail-open 降级）
│   │   ├── constant/LoginCacheConstants.java
│   │   ├── database/MybatisPlusConfig.java
│   │   ├── idgen/IdGeneratorConfig.java
│   │   ├── properties/       # 配置属性类（Jwt / AuditLog / RateLimitFilter / Security / ThreadPool / WebMvc / ImageProcessing / Cache / FileUpload / Idempotency / Lock / SlowSql）
│   │   ├── redis/            # RedisConfig（RedisTemplate 序列化约定）+ RedissonConfig（分布式锁）
│   │   ├── security/SecurityConfig.java
│   │   └── web/              # WebMvcConfig + ResponseAdvice（Result 统一包装）
│   ├── event/                # 领域事件基础设施
│   │   ├── core/ModulithDomainEventPublisher.java  # [@Primary] Modulith 发布器（Outbox 与应用事务同原子）
│   │   ├── dlq/              # DlqRetryScheduler + DlqAnomalyListener（三级重试）
│   │   ├── idempotency/EventIdempotencyChecker.java
│   │   ├── metadata/         # EventMetadata + EventMetadataMessagePostProcessor（traceId 跨 MQ 传递）
│   │   └── metrics/EventMetricsService.java
│   ├── exception/GlobalExceptionHandler.java
│   ├── file/                 # 文件服务：FileController（adapter/inbound/web/）+ FileService + ImageProcessingService + ImageQueryService + storage/（可插拔）
│   ├── idgen/UuidV7IdGenerator.java
│   ├── lock/                 # 分布式锁基础设施（order/payment/超时任务共用）：DistributedLockPort + DistributedRedissonLockAdapter + LockAcquisitionException
│   ├── messaging/            # RabbitMQ 配置：RabbitMQConfig + EventExternalizationConfig
│   ├── metrics/              # BusinessMetricsService + MetricsConfig
│   ├── mybatis/              # SlowSqlInterceptor（500ms 慢 SQL 指标）
│   ├── util/                 # FileUtils / AuditLogUtil / RequestUtil / SecurityContextUtil / LocalRateLimiter / DistributedRateLimiter
│   └── web/                  # Web 层
│       ├── filter/           # RateLimitFilter（限流+防重统一过滤器，配置驱动，替代原 AOP 切面）+ CachedBodyHttpServletRequestWrapper（防重 key 含 body hash）
│       ├── handler/          # CustomMetaObjectHandler（MyBatis-Plus 自动填充）+ LoggingInterceptor（MDC 注入 clientIp/method/uri）
│       ├── idempotency/      # Idempotency-Key 幂等（配置驱动，24h 响应缓存，非 2xx 不缓存，fail-open）
│       ├── cookie/RefreshCookie.java
│       └── ErrorResponseWriter.java
```

#### GlobalExceptionHandler 的定位 `[现状]`

当前 `GlobalExceptionHandler` 放在 `easyorange-framework` 中，原因是：
- 它处理的是所有模块通用的异常映射逻辑（BusinessException → HTTP 响应）
- 它依赖框架层的异常类型（Spring Security、Validation 等）
- 通过 Spring Boot 自动配置注册，所有模块自动生效

`[演进]` 如果未来需要处理模块特有的异常映射（如特定业务错误码的特殊处理），可在 `easyorange-application` 中添加补充的异常处理器，优先级高于 framework 中的通用处理器。

---

### 3. easyorange-message（消息业务模块）`[现状]`

当前 `easyorange-message` 是一个完整的业务限界上下文，负责站内信/通知功能，采用完整 DDD 六边形架构（无分层 `service/` / `entity/` / `dto/` 目录，聚合根在 `domain/aggregate/`，DO 在 `adapter/outbound/persistence/`）：

```
easyorange-message
├── com.cartethyia.easyorange.message
│   ├── adapter                  # 适配器层
│   │   ├── inbound/
│   │   │   ├── job/             # 定时任务
│   │   │   │   └── MessageArchiveTask.java        # 消息归档定时任务
│   │   │   └── web/controller/  # 消息接口 ✅
│   │   │       ├── MessageCommandController.java
│   │   │       └── MessageQueryController.java
│   │   └── outbound/persistence/  # 出站适配器 (MyBatis 实现)
│   │       ├── MessageDO.java
│   │       ├── MessageDataMapper.java
│   │       ├── MessageMapper.java
│   │       ├── MessageRepositoryImpl.java
│   │       ├── MessageQueryRepositoryImpl.java
│   │       ├── OfflineMessageDO.java
│   │       ├── OfflineMessageMapper.java
│   │       ├── OfflineMessageRepositoryImpl.java
│   ├── application             # 应用层（CQRS 模式）
│   │   ├── command/            # 命令处理
│   │   │   ├── DeleteMessageCommand.java
│   │   │   ├── MarkAsReadBatchCommand.java
│   │   │   ├── MarkAsReadCommand.java
│   │   │   ├── MessageCommandHandler.java
│   │   │   ├── RecallMessageCommand.java
│   │   │   ├── SendMessageCommand.java
│   │   │   └── SendSystemMessageCommand.java
│   │   ├── config/             # 领域服务注册（保持 domain 层纯净）
│   │   │   └── MessageDomainServiceConfig.java
│   │   ├── query/              # 查询处理
│   │   │   ├── ConversationQueryHandler.java
│   │   │   ├── MessageQueryHandler.java
│   │   │   └── dto/            # 查询响应 VO
│   │   │       ├── ConversationListVO.java
│   │   │       ├── ConversationVO.java
│   │   │       ├── MessageVO.java
│   │   │       └── UnreadCountVO.java
│   │   └── service/            # 应用层运维/编排服务
│   │       ├── OfflineMessageStoreService.java   # 离线消息存储（应用层编排）
│   │       └── RateLimiterService.java           # 消息发送频率限制
│   ├── constant                # 消息常量
│   │   └── MessageConstant.java
│   ├── domain                  # 领域层
│   │   ├── aggregate/          # 聚合根（原 entity/ 已消除）
│   │   │   ├── Message.java
│   │   │   └── OfflineMessage.java
│   │   ├── event/              # 领域事件
│   │   │   ├── MessageDeletedEvent.java
│   │   │   ├── MessageReadEvent.java
│   │   │   ├── MessageRecalledEvent.java
│   │   │   └── MessageSentEvent.java
│   │   ├── exception/          # 领域异常（模块唯一）
│   │   │   └── MessageDomainException.java
│   │   ├── port/               # 端口接口（隔离外部依赖）
│   │   │   ├── MessageNotifierPort.java
│   │   │   └── UserInfoPort.java
│   │   ├── repository/         # 仓储接口（实现已迁移到 adapter/outbound/）
│   │   │   ├── MessageRepository.java
│   │   │   └── OfflineMessageRepository.java
│   │   ├── port/query/            # 读仓储（读模型为 application 层概念）
│   │   │   └── MessageQueryRepository.java
│   │   ├── service/            # 领域服务（纯领域规则）
│   │   │   └── SensitiveWordFilterService.java
│   │   └── valueobject/        # 值对象
│   │       ├── MessageQuery.java
│   │       ├── UnreadCount.java
│   │       └── UserInfo.java
│   ├── adapter/inbound/web/dto/request/  # 请求 DTO（适配器层）
│   │   ├── QueryMessageRequest.java
│   │   └── WsMessage.java
│   ├── enums                   # 消息枚举
│   │   ├── MessageResultCode.java
│   │   ├── MessageStatus.java
│   │   ├── MessageType.java
│   │   └── ReadStatus.java
│   └── websocket               # WebSocket 支持
│       ├── AuthHandshakeHandler.java
│       ├── ChatWebSocketHandler.java
│       ├── TypingIndicatorService.java
│       ├── WebSocketAttributes.java
│       ├── WebSocketAuthInterceptor.java
│       ├── WebSocketConfig.java
│       ├── WebSocketEventConsumer.java
│       ├── WebSocketEventListener.java
│       └── WebSocketNotifier.java
```

> **迁移完成**: DDD 六边形分层全部到位——MyBatis Repository 实现在 `adapter/outbound/persistence/`，Controller 在 `adapter/inbound/web/controller/`，定时任务在 `adapter/inbound/job/`，应用层（CQRS + 领域服务注册）在 `application/`。无分层的 `service/` / `entity/` / `dto/` 目录均已消除（2026-07-31 死代码清理 + 消息归档任务迁入 `adapter/inbound/job/`、Config 迁入 `application/config/`）。
>
> **演进历史**：
> 1. ~~`controller/` 未迁移到 `adapter/inbound/web/`~~ ✅ 已完成
> 2. ~~`entity/`、`mapper/`、`service/` 未完全迁移~~ ✅ 已完成（`entity/` 拆分为聚合根 + DO，`service/` 死代码删除 + 存活服务迁入 `application/service/`）
> 3. ~~`dto/` 已迁移：request → `adapter/inbound/web/dto/request/`，vo → `application/query/dto/`~~ ✅

#### 消息模块演进规划 `[演进]`

当前消息模块同时承担了「业务消息（站内信）」和「跨模块事件通信基础设施」两重职责。随着业务增长，应拆分为：

| 演进后模块 | 定位 | 内容 |
|-----------|------|------|
| easyorange-message-infra | 跨模块事件通信基础设施 | RabbitMQ Topic Exchange 路由/重试/死信、事件分发 |
| easyorange-notification | 通知业务限界上下文 | 站内信、消息模板、离线消息、WebSocket、订阅管理 |

**已演进**：领域事件已通过 Spring Modulith + RabbitMQ Topic Exchange 实现跨模块通信，废弃了旧 Outbox 和直发 RabbitMQ 模式。应用服务调用 `DomainEventPublisher.publish()` 由 `ModulithDomainEventPublisher`（`@Primary`）代理到 `ApplicationEventPublisher`，Spring Modulith 在数据库 `EVENT_PUBLICATION` 表中持久化事件（与应用事务同原子），事务提交后异步读取并发布到 `eo.domain.events` Exchange。路由键由事件类名自动派生（`ProductCreatedEvent` → `product.created`），无需手动注册。每个消费者独占队列，失败消息路由到 DLQ + 指数退避重试。at-least-once 语义 + 消费者 `EventIdempotencyChecker` 确保精确一次处理。

---

### 4. easyorange-application（启动聚合层）

```
easyorange-application
├── com.cartethyia.easyorange
│   ├── EasyOrangeApplication.java  # 主启动类
│   ├── adapter/
│   │   ├── event/                  # 跨模块事件消费者
│   │   │   ├── OrderNotificationEventConsumer.java  # 订单状态→站内消息
│   │   │   ├── ProductAuditEventConsumer.java       # 审核→站内消息
│   │   │   ├── ReportProcessedEventConsumer.java    # 举报处理→站内消息
│   │   │   ├── AiProductEventConsumer.java          # 商品→AI 估值/文案
│   │   │   └── AiCreditEventConsumer.java           # 交易/举报→信用分重算
│   │   ├── inbound/web/controller/  # 全局接口
│   │   │   ├── HealthController.java           # 健康检查
│   │   │   ├── PlatformStatsController.java    # 平台统计
│   │   │   ├── AiController.java               # AI 功能集合
│   │   │   ├── CreditScoreController.java      # 信用分
│   │   │   └── AdminSearchReindexController.java # 搜索重建索引
│   │   └── outbound/               # 跨模块适配器实现
│   │       ├── elasticsearch/      # ES 搜索索引适配器
│   │       ├── payment/
│   │       │   └── OrderPaymentGatewayAdapter.java
│   │       ├── product/
│   │       │   ├── FavoriteProductInfoAdapter.java
│   │       │   ├── ProductInventoryAdapter.java
│   │       │   ├── ProductQueryAdapter.java
│   │       │   ├── ProductNotificationAdapter.java
│   │       │   └── ProductSearchIndexAdapter.java
│   │       └── user/
│   │           ├── MessageUserInfoAdapter.java
│   │           └── SellerInfoAdapter.java
├── resources
│   ├── application.yaml            # 主配置
│   ├── application-dev.yaml        # 开发环境配置
│   ├── application-prod.yaml       # 生产环境配置
│   ├── logback-spring.xml          # 日志配置
│   └── db/
│       ├── migration/              # Flyway 迁移脚本
│       │   ├── V1__init_schema.sql                     # 所有初始表结构(含索引、约束、注释)，V1已含全量字段
│       │   ├── R__seed_categories.sql                  # 分类种子数据(可重复)
│       │   └── R__seed_payment_config.sql              # 支付渠道种子数据(可重复)
│       └── dev/                    # 开发测试数据
│           └── R__insert_dev_test_data.sql
└── test/
    └── architecture/
        └── ArchitectureRulesTest.java  # 架构守卫测试
```

> **说明** `[现状]`：当前 `HealthController` 和 `PlatformStatsController` 放在 application 模块，因为它们不属于任何特定业务模块。管理端 API 已迁移至独立的 `easyorange-admin` 模块。

---

### 5. easyorange-admin（管理端模块）`[2026-05-11 新增]`

独立的管理端边界模块，承载所有后台管理 API。从 `application/controller/admin/` 迁移而来：

```
easyorange-admin
├── com.cartethyia.easyorange.admin
│   ├── adapter/inbound/web/controller/  # REST 控制器 (9 个)
│   │   ├── AdminDashboardController.java    # 仪表板（统计/待处理/最近动态）
│   │   ├── AdminUserController.java         # 用户管理（列表/详情/状态）
│   │   ├── AdminUserControllerExtension.java # 用户安全（解锁/重置密码/强制下线/角色）
│   │   ├── AdminProductController.java      # 商品管理（列表/详情/状态）
│   │   ├── AdminProductAuditController.java # 商品审核（带原因审核/批量审核）
│   │   ├── AdminOrderController.java        # 订单管理（列表/详情/取消/强制完成/退款/统计）
│   │   ├── AdminCategoryController.java     # 分类管理（CRUD/树形/启禁/删除）
│   │   ├── AdminRatingController.java       # 评价管理（列表/详情/删除）
│   │   └── AdminReportController.java       # 举报管理（列表/详情/处理/批量处理/历史/统计，6个端点）
│   ├── service/                 # 业务服务层 (9 个)
│   │   ├── AdminDashboardService.java
│   │   ├── AdminUserService.java
│   │   ├── AdminUserSecurityService.java
│   │   ├── AdminProductService.java
│   │   ├── AdminProductAuditService.java
│   │   ├── AdminOrderService.java
│   │   ├── AdminCategoryService.java
│   │   ├── AdminRatingService.java          # 评价管理（列表/详情/删除，走 AdminRatingPort + ProductRatingRepository）
│   │   └── AdminReportService.java          # 批量处理(聚合结果)+处理历史+事件发布(processSingleReport/publishProcessedEvent)
│   ├── adapter/inbound/web/assembler/ # DTO 组装器 (2 个)
│   │   ├── AdminReportAssembler.java
│   │   └── AdminRatingAssembler.java
│   ├── domain/enums/             # 领域枚举
│   │   ├── AdminResultCode.java    # B6xxx 管理员错误码（B6001-B6008）
│   │   └── ReportHandleAction.java # 举报处理动作（code/desc/describe）
│   ├── domain/port/              # 跨模块 ACL 端口 (8 个)
│   │   ├── AdminCategoryPort.java
│   │   ├── AdminDashboardPort.java
│   │   ├── AdminOrderPort.java
│   │   ├── AdminProductAuditPort.java
│   │   ├── AdminProductPort.java
│   │   ├── AdminRatingPort.java
│   │   ├── AdminReportPort.java
│   │   └── AdminUserPort.java
│   └── adapter/inbound/web/dto/  # 数据传输对象 (38 个)
│       ├── request/              # 15 个请求 DTO（含 BatchHandleRequest, @Pattern校验）
│       └── response/             # 23 个响应 Response（含 BatchHandleResultResponse）
```

**API 路由总览：**

| 模块 | 路由前缀 | 接口数 |
|------|---------|--------|
| 仪表板 | `/api/admin/dashboard` | 4 |
| 用户管理 | `/api/admin/users` | 7 (3 原有 + 4 新增) |
| 商品管理 | `/api/admin/products` | 5 (3 原有 + 2 新增) |
| 订单管理 | `/api/admin/orders` | 6 (全新) |
| 分类管理 | `/api/admin/categories` | 6 (全新) |
| 评价管理 | `/api/admin/reviews` | 3 (列表/详情/删除) |
| 举报管理 | `/api/admin/reports` | 6 (列表/详情/处理/批量处理/历史/统计) |

**设计原则：**
- 所有跨模块依赖标记 `<optional>true</optional>`，编译期隔离
- 敏感操作（订单干预、角色切换等）要求 `reason` 字段必填
- 与其他业务模块组织方式一致，保持 DDD 风格

**Maven 依赖：**
```
admin → common, user, product, order, payment (全部 optional)
```

---

## 二、业务模块结构

以 `easyorange-user` 为例，其他业务模块直接照搬此结构。遵循「六边形架构 + DDD」：

```
easyorange-user
├── com.cartethyia.easyorange.user
│   ├── adapter                 # 六边形适配器层（内外交互）
│   │   ├── inbound             # 输入适配器（外部→模块）
│   │   │   └── web             # REST 接口
│   │   │       ├── controller  # 控制器
│   │   │       │   ├── AuthController.java
│   │   │       │   └── UserController.java
│   │   │       ├── dto         # 接口入参/出参
│   │   │       │   ├── request
│   │   │       │   │   ├── auth/              # 认证 + 密码管理
│   │   │       │   │   │   ├── PasswordLoginRequest.java
│   │   │       │   │   │   ├── SmsLoginRequest.java
│   │   │       │   │   │   ├── RegisterRequest.java
│   │   │       │   │   │   ├── RefreshTokenRequest.java
│   │   │       │   │   │   ├── PasswordResetRequest.java
│   │   │       │   │   │   └── ChangePasswordRequest.java
│   │   │       │   │   └── profile/           # 用户资料
│   │   │       │   │       └── UpdateUserRequest.java
│   │   │       │   └── response
│   │   │       │       ├── UserResponse.java
│   │   │       │       └── UserProfileResponse.java
│   │   │       └── validation  # 入参校验
│   │   │           ├── Password.java
│   │   │           ├── PasswordValidator.java
│   │   │           ├── Unique.java
│   │   │           └── UniqueFieldValidator.java
│   │   └── outbound            # 输出适配器（模块→外部，实现输出端口）
│   │       ├── persistence     # 数据库适配（实现仓储端口）
│   │       │   ├── UserDO.java        # 数据库实体（DO）
│   │       │   ├── UserMapper.java    # MyBatis Mapper
│   │       │   └── UserRepositoryImpl.java  # 仓储实现
│   │       ├── cache           # 缓存适配
│   │       │   ├── RedisLoginAttemptAdapter.java
│   │       │   └── RedisSmsCodeAdapter.java
│   │       ├── storage         # 文件存储适配
│   │       │   └── LocalAvatarFileStorage.java
│   │       └── util            # 基础设施工具
│   │           └── NicknameGenerator.java
│   ├── application             # 应用层（用例编排，事务管理）
│   │   ├── dto                 # 应用层 DTO（被 assembler/service/controller 共用）
│   │   │   ├── UserResponse.java
│   │   │   └── UserProfileResponse.java
│   │   ├── assembler           # 对象转换器
│   │   │   └── UserAssembler.java
    │   │   └── service             # 应用服务（按用例聚合，合理边界）
│   │       ├── AuthAppService.java              # 认证+密码管理（注册/登录/登出/刷新/忘记密码/修改密码）
│   │       └── ProfileAppService.java           # 用户信息管理（信息/更新/头像）
│   ├── domain                  # 领域层（最纯净，无框架依赖）
│   │   ├── aggregate           # 聚合根（领域核心）
│   │   │   └── User.java
│   │   ├── constant            # 领域常量
│   │   │   ├── UserConstant.java
│   │   │   └── UserSecurityConstant.java
│   │   ├── valueobject         # 模块私有值对象（必须不可变）
│   │   │   ├── ContactInfo.java    # 联系方式（email, phone，含格式校验）
│   │   │   ├── PersonalInfo.java   # 个人基础信息（realName, nickName, sex）
│   │   │   ├── DisplayProfile.java # 展示信息（avatar）
│   │   │   ├── LoginInfo.java
│   │   │   └── AuditInfo.java
│   │   ├── service             # 领域服务（纯业务逻辑，无状态，无框架注解）
│   │   │   ├── AuthenticationService.java
│   │   │   ├── LoginSecurityService.java
│   │   │   └── RegistrationService.java
│   │   ├── enums               # 领域业务枚举
│   │   │   ├── UserStatus.java
│   │   │   ├── UserType.java
│   │   │   ├── Sex.java
│   │   │   ├── LoginMethod.java
│   │   │   ├── ClientType.java
│   │   │   ├── DelFlag.java
│   │   │   └── UserResultCode.java
│   │   ├── port                # 六边形端口（扁平，已去 output/ 子包）
│   │   │   ├── AvatarFilePort.java      # 头像文件端口
│   │   │   ├── LoginAttemptPort.java    # 登录尝试端口
│   │   │   ├── PasswordEncoderPort.java # 密码编码端口
│   │   │   ├── SmsCodePort.java         # 短信验证码端口（含限流）
│   │   │   ├── SmsSenderPort.java
│   │   │   ├── UserQueryPort.java
│   │   │   └── AdminUserManagementPort.java
│   │   └── repository          # 仓储端口（聚合根存取接口）
│   │       └── UserRepository.java
│   └── config                  # 模块专属配置（归入基础设施关注点）
│       └── UserDomainConfig.java  # 领域服务 Bean 配置
```

**包结构调整说明：**

- 去掉了 `infrastructure` 层，所有端口实现统一归入 `adapter/outbound`（纯粹的六边形架构）
- 去掉 `domain/port/input`（输入端口），当只有一个实现类时属于过度抽象（YAGNI 原则）
- `config` 包属于基础设施关注点，保留在模块根包下
- `application/service` 替代 `application/command|query`，采用传统应用服务模式；读写差异大时再拆分为 CQRS
- `domain/port/` 下放置所有出站端口接口（`OutboundPort` 标记接口已全库移除，2026-05-27 去 `output/` 子包时一并删除）
- 包结构采用扁平的 `domain/port/`（2026-05-27 统一移除 `output/` 子包），所有出站端口接口平铺在 `domain/port/` 下
- `domain/repository/` 作为特殊的出站端口，专门定义聚合根的存取接口
- 领域常量放在 `domain/constant/`，技术常量（路由前缀、缓存 Key、MQ Topic）应下沉到对应适配器层
- 领域服务不使用 `@Service` 等框架注解，通过 `config/` 下的配置类手动注册 Bean
- 领域服务依赖外部功能时，通过端口接口抽象，由适配器层实现
- **应用服务按职责聚合**：避免过度拆分。当多个用例共享相同的依赖集和事务边界时，合并到同一个应用服务（如 `AuthAppService` 处理注册/登录/登出/刷新/忘记密码/修改密码）。当用例差异大或可能独立演进时再拆分。核心标准：**一个应用服务的构造函数变更不应强迫修改不相关的调用方**
- **DTO 分层放置**：当 Response/DTO 被 `application` 层（assembler、service）和 `adapter` 层（controller）共同引用时，必须放在 `application/dto/` 下，禁止放在 `adapter/inbound/web/dto/response/` 中。原因是 application 层依赖 adapter 层会违反六边形架构的依赖倒置原则（DIP）。仅由 controller 单独使用的响应 DTO 可保留在 adapter 层

> **现状**：所有业务模块已统一采用扁平的 `domain/port/` 结构（2026-05-27）。`infrastructure/` 包已废弃，相关实现全部迁入 `adapter/outbound/`。
>
> **缓存统一规范**：所有业务模块的缓存适配器必须遵循以下规则：
> 1. **端口定义在 `domain/port/`**：如 `OrderCachePort`、`ProductCachePort`
> 2. **实现放在 `adapter/outbound/cache/`**：如 `RedisOrderCacheAdapter`、`ProductCacheAdapter`
> 3. **禁用 `infrastructure/` 包**：ArchitectureRulesTest 会对任何新建的 `infrastructure/` 包路径文件报错
> 4. **技术常量下沉到适配器层**：Redis Key 前缀、过期时间等放在 `adapter/outbound/cache/` 下的常量类中
> 5. **应用层只依赖端口**：通过 `OrderCachePort` 等接口操作缓存，不直接依赖 `RedisTemplate`

---

## 三、其他业务模块结构参考

### easyorange-product `[现状]`

product 模块已采用完整 DDD 分层 + CQRS 模式：

```
easyorange-product
├── adapter/inbound/web/
│   ├── controller/           # 控制器
│   │   ├── ProductController.java
│   │   ├── ProductReportController.java
│   │   ├── ProductRatingController.java
│   │   └── ProductSearchController.java
│   └── dto/
│       ├── request/          # 请求 DTO
│       └── response/         # 响应 DTO
├── adapter/outbound/
│   ├── cache/                # 缓存适配器（实现缓存端口）
│   │   ├── ProductCacheAdapter.java
│   │   ├── CategoryCacheAdapter.java
│   │   └── ProductCacheConstant.java
│   ├── scheduler/
│   │   └── ViewCountFlushScheduler.java  # 浏览量 Redis→DB 定时刷入
│   └── persistence/
│       ├── converter/        # DO 转换器
│       ├── persistence/      # 数据对象
│       ├── mapper/           # MyBatis Mapper
│       ├── repository/       # 仓储实现
│       └── ProductSnapshotAdapter.java
├── application/              # 应用层（CQRS 模式）
│   ├── command/              # 命令侧（顶层 record + 密封接口，见 DDD规范.md）
│   │   ├── ProductCommand.java            # 密封接口
│   │   ├── CreateProductCommand.java      # 顶层 record
│   │   ├── UpdateProductCommand.java      # 顶层 record
│   │   ├── CreateProductRatingCommand.java# 顶层 record
│   │   ├── ProductCommandHandler.java
│   │   ├── ProductReportCommandHandler.java
│   │   └── ProductRatingCommandHandler.java
│   ├── event/                # 事件监听
│   │   └── ProductEventConsumer.java
│   ├── query/                # 查询侧（CQRS Read）
│   │   ├── ProductQueryHandler.java
│   │   ├── ProductReportQueryHandler.java
│   │   ├── ProductRatingQueryHandler.java
│   │   ├── ProductSearchQueryHandler.java
│   │   ├── ProductSearchCriteria.java
│   │   ├── CategoryQueryHandler.java
│   │   ├── dto/              # 应用层输出 VO
│   │   │   ├── ProductVO.java
│   │   │   ├── ProductRatingVO.java
│   │   │   ├── RatingStatsVO.java
│   │   │   └── ProductSearchResult.java
│   │   ├── readmodel/        # 读模型（Repository 返回）
│   │   │   ├── ProductReadModel.java
│   │   │   ├── CategoryReadModel.java
│   │   │   ├── SellerReadModel.java
│   │   │   ├── HotKeywordReadModel.java
│   │   │   └── SearchHistoryReadModel.java
│   │   └── assembler/
│   │       └── ProductReadModelAssembler.java
│   └── service/              # 应用服务
│       ├── ProductViewCountAppService.java    # 浏览量 Redis 增量
│       ├── ViewCountBatchProcessor.java       # 浏览量批量刷入 DB
│       └── SearchHistoryBufferAppService.java
├── config/                   # 模块配置
└── domain/                   # 领域层
    ├── aggregate/
    ├── constant/
    ├── entity/
    │   ├── ProductReport.java      # 举报领域实体（不可变）
    │   └── ReportHandleHistory.java # 处理历史实体（含reconstitute工厂方法）
    ├── enums/
    │   ├── ProductStatus.java
    │   ├── ProductReportStatus.java # 4状态: PENDING(0)/PROCESSING(1)/RESOLVED(2)/DISMISSED(3)
    │   └── ReportReasonType.java    # 4类型: FAKE_INFO(1)/INFRINGEMENT(2)/VIOLATION(3)/OTHER(4)
    ├── event/
    │   ├── ProductAuditedEvent.java
    │   └── ReportProcessedEvent.java # 举报处理完成事件(record)
    ├── exception/
    ├── port/
    ├── repository/
    │   └── ReportHandleHistoryRepository.java # 处理历史仓储接口
    ├── service/
    │   └── ProductReportDomainService.java  # 举报处理编排（异常统一走 ProductDomainException）
    └── valueobject/
```

> **SellerInfoPort** `[已完成]`：product 模块通过 `SellerInfoPort` 端口 + `SellerInfo` 值对象隔离 user 模块依赖，适配器在 application 模块实现。

---

### easyorange-favorite `[现状]`

favorite 模块已完成 DDD 六边形架构迁移：

```
easyorange-favorite
├── adapter/                    # 适配器层
│   ├── inbound/web/            # 入站适配器
│   │   ├── controller/
│   │   │   └── FavoriteController.java
│   │   └── dto/request/
│   │       ├── BatchCheckRequest.java
│   │       └── BatchRemoveRequest.java
│   └── outbound/persistence/   # 出站适配器
│       ├── FavoriteDO.java
│       ├── FavoriteMapper.java
│       └── FavoriteRepositoryImpl.java
├── application/                # 应用层
│   ├── dto/
│   │   ├── AddFavoriteDTO.java
│   │   ├── FavoritePageQuery.java
│   │   ├── FavoriteResponse.java
│   │   └── RemoveFavoriteDTO.java
│   └── service/
│       └── FavoriteService.java
└── domain/                     # 领域层
    ├── aggregate/
    │   └── Favorite.java
    ├── port/
    │   └── ProductInfoPort.java
    ├── repository/
    │   └── FavoriteRepository.java
    └── valueobject/
        ├── ProductDetailInfo.java
        ├── ProductInfo.java
        └── SellerInfo.java
```

> **事务规范**: 查询方法必须标注 `@Transactional(readOnly = true)` 以启用数据库只读优化。

---

### easyorange-order `[现状]`

order 模块已采用完整 DDD 分层（下单链路为本地单事务 + 分布式锁 + Outbox，拒绝 Saga，见 [ADR-0007](../adr/0007-order-local-tx-over-saga.md)）：

```
easyorange-order
├── adapter/inbound/
│   ├── job/                   # 定时任务
│   │   ├── OrderAutoConfirmTask.java
│   │   └── OrderTimeoutTask.java
│   ├── mq/                   # 消息订阅
│   │   └── subscriber/
│   │       └── OrderLifecycleEventConsumer.java  # 订单生命周期（取消/退款恢复库存, 完成标记售出）
│   └── web/
│       ├── controller/
│       │   ├── OrderCommandController.java
│       │   └── OrderQueryController.java
│       ├── assembler/
│       │   └── OrderCommandAssembler.java  # Request DTO → Command
│       └── dto/
│           └── request/        # 仅请求 DTO（响应 Response 已移至 application/dto/）
├── adapter/outbound/
│   ├── cache/                 # 缓存适配器
│   │   └── RedisOrderCacheAdapter.java
│   ├── messaging/             # 跨模块适配器
│   │   ├── PaymentGatewayAdapter.java
│   │   ├── ProductInventoryAdapter.java
│   │   ├── ProductQueryAdapter.java
│   │   └── UserInfoAdapter.java
│   └── persistence/
│       ├── OrderRepositoryImpl.java
│       ├── OrderQueryRepositoryImpl.java
│       ├── OrderDO.java
│       ├── OrderDataMapper.java   # 手写 Component: DO ↔ Domain
│       ├── OrderMapper.java
│       └── OrderItemDO.java / OrderItemMapper.java
├── application/
│   ├── command/               # 命令处理（OrderCommandHandler 唯一执行器，见 ADR-0007）
│   │   ├── OrderCommandHandler.java  # 一命令一具名用例方法（createOrder/payOrder/…，@Transactional + 锁）
│   │   ├── OrderItemPreparer.java     # 商品数据准备
│   │   └── CreateOrderCommand.java   # 命令 record（其余命令 record 同目录）
│   ├── dto/
│   │   └── OrderVO.java            # 响应 VO
│   └── query/                 # 查询处理
│       ├── OrderQueryHandler.java
│       └── assembler/
│           └── OrderReadModelAssembler.java  # ReadModel → OrderVO
├── domain/
│   ├── aggregate/
│   │   ├── Order.java
│   │   ├── OrderCreateSpec.java            # record 收敛 createOrder() 工厂参数
│   │   └── OrderReconstructSpec.java       # record 收敛 from() 重建参数
│   ├── constant/
│   ├── event/
│   ├── exception/
│   │   └── OrderDomainException.java        # 唯一领域异常（of / notFound / upstream 具名工厂）
│   ├── port/
│   │   ├── OrderCachePort.java
│   │   ├── OrderQueryCondition.java
│   │   ├── PaymentGatewayPort.java
│   │   ├── ProductInventoryPort.java
│   │   ├── ProductQueryPort.java
│   │   └── UserInfoPort.java
│   ├── port/query/
│   │   └── OrderQueryRepository.java
│   ├── repository/
│   │   └── OrderRepository.java
│   └── valueobject/
```

> **已完成**：order 模块对 product、user、payment 的 Maven 依赖均已标记为 `<optional>true</optional>`，业务逻辑通过端口接口 + 适配器模式完全隔离。

---

### easyorange-payment `[现状]`

payment 模块已采用完整 DDD 分层：

```
easyorange-payment
├── adapter/inbound/web/
│   ├── assembler/
│   ├── request/
│   ├── response/
│   ├── MockPaymentController.java
│   ├── PaymentCommandController.java
│   └── PaymentQueryController.java
├── adapter/outbound/
│   ├── gateway/
│   │   └── PaymentGatewayAdapter.java
│   └── persistence/
│       ├── converter/
│       ├── mapper/
│       ├── po/
│       ├── IdempotencyKeyRepositoryImpl.java
│       ├── PaymentRepositoryImpl.java
│       └── PaymentConfigRepository.java
├── application/
│   ├── command/               # 命令处理
│   ├── event/                 # [已迁移] 事件处理已迁移到 RabbitMQ 消费者
│   ├── idempotency/           # 幂等性服务
│   ├── lock/                  # 分布式锁
│   ├── metrics/               # 指标服务
│   ├── mock/                  # Mock 支付
│   └── query/                 # 查询处理
├── constant/
│   └── PaymentConstant.java
├── domain/
│   ├── aggregate/
│   │   └── Payment.java
│   ├── constant/
│   │   ├── PaymentMethod.java
│   │   ├── PaymentResultCode.java
│   │   └── PaymentStatus.java
│   ├── event/
│   ├── exception/
│   ├── factory/
│   ├── port/
│   ├── repository/
│   ├── saga/
│   ├── specification/
│   └── valueobject/
```
