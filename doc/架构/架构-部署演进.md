# EasyOrange 部署与演进规划

> 本文档定义了 EasyOrange 项目的落地步骤和配置管理规范。

---

## 一、落地步骤（循序渐进）

### 第一步：完善 easyorange-common `[现状]` ✅

已完成：
- 通用工具、统一响应体、基础异常、领域事件接口整理完毕
- 清理不必要的框架依赖（BaseDO、RequestUtil、FileUtils 已迁移到 framework）
- PageResult 改为纯 Java record

~~待优化 `[演进]`~~ ✅ 已完成：
- ~~将 `PageRequest` 中的 MyBatis-Plus 依赖迁移到 framework，使 common 完全无 ORM 框架依赖~~ common 模块已无 MyBatis/IBatis 导入

---

### 第二步：完善 easyorange-framework `[现状]` ✅

已完成：
- MyBatis、Redis、Security 的通用配置和基类写好
- 业务模块可直接继承使用，无需重复写配置
- 删除了废弃的 CacheUtils

---

### 第三步：完善 easyorange-user 模块 `[现状]` ✅

已完成 DDD 架构优化：
- 领域层完全纯净，无框架依赖
- 领域服务不使用 `@Service` 注解，通过配置类注册 Bean
- `infrastructure` 包迁移到 `adapter/outbound/util/`
- 领域常量移至 `domain/constant/`
- 新增 `PasswordEncoderPort` 端口抽象密码编码功能

---

### 第四步：完善 easyorange-product 模块 `[现状]` ✅

已完成 DDD 架构优化：
- 领域服务 `ProductReportDomainService` 移除框架注解，通过 `ProductDomainConfig` 注册 Bean
- 缓存服务迁移到 `adapter/outbound/cache/`，改名为 Adapter
- 技术常量分离到 `adapter/outbound/cache/ProductCacheConstant.java`
- 领域层 `ProductConstant` 仅保留业务常量

---

### 第五步：消除业务模块间的 Maven 直接依赖 ✅ 已完成

所有跨模块 Maven 依赖已标记为 `<optional>true</optional>`，跨模块调用通过端口接口 + 适配器模式完全隔离：
1. ✅ 确保所有跨模块调用通过端口接口 + 适配器模式隔离
2. ✅ 将 Maven 直接依赖改为 `<optional>true</optional>`
3. 写操作通过领域事件驱动解耦，查询操作保留同步端口调用

---

### 第六步：补全 favorite 模块的 DDD 分层 ✅ 已完成

favorite 模块已演进为完整的 adapter/application/domain 分层（adapter/inbound/web + adapter/outbound/persistence + application/service + domain/aggregate + domain/repository + domain/valueobject + domain/port）。

---

### 第七步：迁移 message 模块到标准 DDD 分层 `[演进]`

- ~~将 `controller/` 迁移到 `adapter/inbound/web/`~~ ✅ 已完成
- 将 `domain/repository/` 中的 MyBatis 实现类迁移到 `adapter/outbound/persistence/`
- 将 `entity/`、`mapper/` 迁移到 `adapter/outbound/persistence/`
- 将 `service/` 迁移到 `application/service/`
- 将 `dto/` 迁移到 `adapter/inbound/web/dto/`

---

### 第八步：引入 RabbitMQ 消息队列 ✅ 已完成

跨模块事件通信从 Spring EventBus + @Async 迁移到 RabbitMQ（Topic Exchange + Quorum Queue + 手动 ACK）。框架层 `framework/messaging/` 提供统一发布器、路由键解析、可靠性回调（ConfirmCallback/ReturnCallback）。后续已演进为 RabbitMQ-only 模式，EventBus 回退已移除。

---

### 第九步：common 模块拆分 `[经评估暂缓]`

当业务增长到需要区分技术工具和领域共享时，将 `easyorange-common` 拆分为 `easyorange-common-tech` 和 `easyorange-common-domain`。

> **评估结论（2026-07-04）**：暂缓拆分。common 模块 30 个源文件中仅 3 个真正属于 domain 类（`Money`、`DomainEvent`、`DomainEventPublisher`），且 `Money` 依赖 `BizRequire`（TECH 工具），拆分后 domain 模块将违反"无框架依赖"原则。100+ 处导入需审查，10+ 模块 pom.xml 需更新，收益低于成本。重新评估时机：domain 类增长到 10+ 个，或出现明确的反模式。

---

### 第十步：消除架构测试白名单 `✅ 已完成（2026-07-04）`

`ArchitectureRulesTest` 中的白名单条目已全部消除（`MessageQueryRepository` / `PaymentQueryRepository` / `CallbackSignatureVerifierPort`），`PORT_ALLOWLIST = Set.of()`，领域层零框架依赖。

---

## 二、配置管理规范

### 环境配置分层

| 配置文件 | 用途 | 覆盖内容 |
|---------|------|---------|
| `application.yaml` | 基础配置 | 数据源、Redis、MyBatis-Plus、线程池、通用安全配置、虚拟线程、Micrometer Tracing |
| `application-dev.yaml` | 开发环境 | 较小连接池、Flyway 开发数据、JWT 开发密钥、详细日志 |
| `application-prod.yaml` | 生产环境 | 大连接池、SSL、严格安全、JSON 日志、Swagger 禁用 |

### 敏感配置管理

| 配置项 | 管理方式 | 说明 |
|--------|---------|------|
| `JWT_RSA_PRIVATE_KEY` | 环境变量 | 生产环境必须设置 RSA 私钥 PEM 路径 |
| `JWT_RSA_PUBLIC_KEY` | 环境变量 | 生产环境必须设置 RSA 公钥 PEM 路径 |
| `EASYORANGE_DB_PASSWORD` | 环境变量 | 生产环境必须设置 |
| `REDIS_PASSWORD` | 环境变量 | 生产环境必须设置 |
| `RABBITMQ_PASSWORD` | 环境变量 | 生产环境必须设置 |
| `CORS_ALLOWED_ORIGINS` | 环境变量 | 生产环境必须设置白名单 |

**原则**：
- 开发环境提供默认值便于本地启动
- 生产环境必须通过环境变量设置，无默认值
- 禁止在代码或配置文件中硬编码密钥

### JVM 配置

```bash
# 生产环境 Docker 配置
JAVA_OPTS="-Xms256m -Xmx2g -XX:+UseG1GC -XX:+UseStringDeduplication"
SPRING_PROFILES_ACTIVE=prod
```

| 参数 | 说明 |
|------|------|
| `-Xms256m` | 初始堆内存 |
| `-Xmx2g` | 最大堆内存 |
| `-XX:+UseG1GC` | 使用 G1 垃圾回收器 (JDK 25 默认) |
| `-XX:+UseStringDeduplication` | G1 字符串去重，节省堆内存 |
| 优雅停机 | `server.shutdown=graceful`，超时 90s |

### compose.yaml 开发环境

```bash
# 一键启动开发依赖 (MySQL 8.4.11 + Redis 8.10.0 + RabbitMQ 4.3.4)
docker compose -f compose.yaml up -d
```

compose.yaml 中的中间件连接参数由显式 env 直连（TD-001：@ServiceConnection 自动发现机制已移除）。凭据与端口统一写成 `${VAR:-default}` 并经仓库根 `.env` 插值（缺省走内置默认值，故无 `.env` 也能零配置启动），`MYSQL_USER/MYSQL_PASSWORD` 与应用的 `EASYORANGE_DB_USERNAME/_PASSWORD` 同源；k8s overlay 的 backend.env.example / infra.env.example 同口径标注「必须一致」。

### Docker 多阶段构建

```bash
# 构建命令
cd easyorange-backend
DOCKER_BUILDKIT=1 docker build -f Dockerfile -t easyorange-backend:latest .
```

**镜像特点**：
- 两阶段构建：builder (JDK) → runtime (JRE)
- 非 root 用户运行 (`easyorange:easyorange`)
- 健康检查使用 wget (Alpine 自带，无额外依赖)
- 时区 `Asia/Shanghai`

---

## 三、Kubernetes 部署（kustomize）

> **定位**：K8s 只编排**无状态应用层**（backend + frontend）。本地开发与集成测试继续走 docker compose 三件套（`*IT` 测试依赖 compose 栈），两者互不替代。

### 目录

```
k8s/
├── base/                    # 无状态应用层（Deployment/Service/HPA/PDB/PVC/Ingress/NetworkPolicy/ConfigMap）
├── overlays/demo/           # k3s 单机自包含演示（含 MySQL/Redis/RabbitMQ StatefulSet，demo-only）
├── observability/           # ServiceMonitor + PrometheusRule（需 kube-prometheus-stack）
└── scripts/                 # JWT 密钥生成
```

### 实践要点

- **零中断滚动**：`maxUnavailable=0` + `maxSurge=1` + 优雅停机（Spring `server.shutdown=graceful` 90s，`terminationGracePeriodSeconds=120`）
- **探针三件套**：startup=`/actuator/health` / readiness=`/actuator/health/readiness`（db/redis/rabbit/ai 就绪组，base 配置已开 `probes.enabled=true`）/ liveness=`/actuator/health/liveness`（仅 JVM）
- **HPA**：CPU 70% 基准 2→6 副本（Java 内存不进 HPA，GC 掩盖真实压力）
- **PDB**：`minAvailable=1`；**Pod Security**：命名空间 baseline（infra 需 root），app 满足 restricted
- **最小权限**：非 root（backend 1000 / nginx 101）+ drop ALL cap + seccomp RuntimeDefault + 只读根文件系统 + `automountServiceAccountToken=false`
- **零信任网络**：默认拒绝 + 显式放行（NetworkPolicy；k3s flannel 不强制，Cilium/kube-router 下自动生效）
- **配置分层**：非敏感→ConfigMap；敏感→kustomize secretGenerator（内容 gitignore，模板见 `*.example`）；JWT RSA PEM→Secret 只读挂载
- **可观测性**：`k8s/observability`（ServiceMonitor 抓 `/actuator/prometheus` + Down/5xx/P99 告警）

### 部署

```bash
# 构建镜像 → 导入 k3s → 生成 JWT 密钥 + 填写 env 文件 → 部署
bash easyorange-backend/keys/generate-rsa-keypair.sh k8s/overlays/demo/env/jwt
kubectl apply -k k8s/overlays/demo
```

完整步骤 + 生产演进清单（托管中间件/TLS/ExternalSecret/RWX 存储/GitOps）见 [k8s/README.md](../../k8s/README.md)。
