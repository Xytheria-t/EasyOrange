# EasyOrange 工程指标

> 本文档是项目所有可量化数字的**单一权威来源（single source of truth）**。其他文档（README / PRODUCT_DIRECTION / CLAUDE.md / 前端 README 等）一律引用本文档，不再独立陈述数字。
>
> 用途：项目展示与讲解的"硬数字弹药库"，所有数字均可本地复现。
>
> 版本：v1.12 · 最近核对：2026-08-22 · 核对方式：`grep` + 项目源代码静态统计 + WSL2 实测

---

## 一、已验证的硬数字（可立即引用）

> 以下数据均通过 `grep` 在源代码中直接核对，可在本地复现。
>
> **2026-08-22 整体收口实测**（企业级差距优化批次后，`./mvnw test` 全量）：后端 surefire **1,673** 用例全绿（0 失败 / 0 错误 / 0 跳过）+ OrderCreationIT 4 用例（failsafe，真实 MySQL/Redis/RabbitMQ）；前端 Vitest 实测 **1,029** / 111 文件，Playwright E2E **41** 用例全绿。README 取整锚点 **2,400+**（后端 1,673 + 前端 1,029 = 2,702，下限仍成立）。
>
> **2026-08-02 实测全绿（v8 — order 状态机动作驱动重构后）**：前端 **1,056**；后端 **1,356**（`mock-maker-subclass` 模式绕过 WSL2 ByteBuddy attach 限制）。企业级差距优化阶段补齐：审计日志 Outbox 事件化 + DLQ 三级重试调度器 + Redisson 分布式令牌桶 + 拒绝 Saga 决策（ADR-0007：本地单事务 + 分布式锁 + Outbox）+ TokenBudget 6 场景接入 + Prompt 6 YAML 化 + OpenAPI 3 契约。order 状态机重构为动作驱动（`OrderAction` 单一事实来源）。

### 1.1 规模与解耦

| 指标 | 数值 | 验证命令 |
|---|---|---|
| Maven 模块数 | **11** | `ls easyorange-backend/easyorange-* -d \| wc -l` |
| 跨模块 Port 接口数 | **35** | `grep -rhE "interface \w+Port" easyorange-backend --include="*.java" \| grep -v /test/ \| wc -l`（含泛型接口 `OrderCachePort<T>` 与 order/payment 各自的 `PaymentGatewayPort`，需用 `interface \w+Port` 而非 `\w+Port {`；2026-08-14 命名收口：读仓储去 `RepositoryPort` 双后缀、新增 `ViewCountPort`，总数维持 35） |
| RabbitMQ 事件消费者 | **12** | 12 个 `@RabbitListener` 业务事件消费者（2026-08-17 新增 FavoritePriceDrop / PaymentSucceeded 两个）+ 1 个 `DlqAnomalyListener`（单监听同时看 12 个 DLQ 队列） |
| 数据库表数 | **32** | `grep -c "CREATE TABLE" V1__init_schema.sql` = 32 张现役（V2 仅加列不加表；2026-08-15 开发阶段合并 V1~V9 为单文件，2026-08-17 起按演进期拆增量） |
| Flyway 迁移脚本 | V1（32 表）+ V2（收藏价格快照）+ V3（消息清理/订单定时扫描索引）+ 3 个 R（+ dev seed） | `db/migration/`（V2 为 2026-08-17 收藏降价功能首个增量脚本，TD-012 已还）+ `db/dev/R__insert_dev_test_data.sql`（R__seed_knowledge_docs 为 RAG 知识库种子，kb-0001~0005 供金标准集引用） |
| DDD 聚合根 | Product / Order / Payment / User | 4 个核心聚合根 |

### 1.2 测试规模

| 指标 | 数值 | 验证方式 |
|---|---|---|---|
| 后端测试方法数（注解统计） | **1,673** | `./mvnw test` 实测全绿（2026-08-22，0 失败 / 0 错误 / 0 跳过） |
| 后端测试用例数（含参数化展开） | **1,673** | 同上（surefire 实测口径） |
| 前端测试用例数 | **1,029** | `npm run test`（Vitest）实测全绿（2026-08-19，111 文件；死代码清理连带删减对应测试） |
| **总测试用例数** | **2,702（锚点 2,400+）** | 后端 1,673 + 前端 1,029；README 取整锚点下限，收口实测后仍成立 |
| 后端测试文件数 | **221**（另 7 个 `*IT.java`） | `find easyorange-backend -name "*Test.java" \| wc -l`（2026-08-22 复核：221 单测文件 + 7 集成测试文件，IT 含 MessagingRoundTripIT / FavoriteRepositoryIT / **OrderCreationIT（下单链路：原子落库/库存回滚/8 并发防超卖）** 等） |
| 前端测试文件数 | **111** | `find easyorange-frontend/src \( -name "*.test.ts" -o -name "*.test.tsx" -o -name "*.spec.ts" -o -name "*.spec.tsx" \) \| wc -l`（2026-08-19 复核） |
| 架构守卫测试 | ArchUnit `ArchitectureRulesTest`（**10 条规则**） | `easyorange-application/src/test/java/.../architecture/` |
| 变异测试（PIT 1.25.9，全模块 domain 层实测） | **全量 765 变异 / 约 3 分钟跑完**（CI nightly 完全跑得起）：order mutation **76%** / test strength **78%** / line **93%**（75 变异）；product mutation **64%** / test strength **74%** / line **92%**（281 变异）；payment 84%/92%、message 76%/91%、user 69%/80% | `./mvnw -Ppit test-compile pitest:mutationCoverage`，HTML 报告 `target/pit-reports/index.html` |

> **1,672 vs 注解统计说明**：源码里 `@Test`/`@ParameterizedTest`/`@RepeatedTest` 注解物理出现 1,630 次；surefire 跑出的实际用例数 1,672（参数化测试按参数个数展开，部分继承类/嵌套类差异）。两个口径都对，含义不同。
>
> **后端 WSL2 测试说明**：已配置 `mock-maker-subclass` 模式绕过 WSL2 下 ByteBuddy attach 限制，后端测试全绿（2026-08-19 实测 1,672）。原生 Linux / CI 环境无需该配置。
>
> **变异测试说明**：PIT 向 domain 层注入变异（聚合根状态机/领域服务/值对象），用现有测试杀灭变异来评估测试真实质量 — 行覆盖率的"金标准"补充。默认 `-Ppit` profile 按需启用（较慢），阈值门禁默认 0 不阻断，CI 用 `-Dpit.mutationThreshold=60 -Dpit.testStrengthThreshold=75 -Dpit.coverageThreshold=70` 启用。除上表 5 个核心模块外，admin/common/favorite 轻量 domain 模块亦有报告（变异基数小：admin 18%/40%、common 75%/79%、favorite 31%/94%）。

### 1.3 AI 工程化深度

> **8 件套全落地**（2026-07-30 升级）：6 个 AI 决策点全部接入 `@TokenBudget` 日预算治理 + Prompt 6 个 YAML 版本化 + **轻量级 Agent 编排（4 路并行 Tool Calling）**。

| 指标 | 数值 | 位置 |
|---|---|---|
| AI 决策点 | **6 个** | 见 [doc/集成/AI-资产管理.md](./集成/AI-资产管理.md) |
| AI 框架化 | **Spring AI 2.0**（`chatModel` @Primary DeepSeek / `visionChatModel` Qwen-VL / `embeddingModel` DashScope text-embedding-v3） | [easyorange-ai/config/AiModelConfig.java](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/config/AiModelConfig.java) |
| 调用去重工具 | `AiModelSupport`（callText / callJson / embed / analyzeImages） | [easyorange-ai/service/AiModelSupport.java](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/AiModelSupport.java) |
| Embedding 真实现 | 查询侧 kNN + 索引侧 `nameEmbedding` 写入（dims=1024 与 ES `dense_vector` 对齐） | [ElasticsearchProductSearchIndexAdapter](../easyorange-backend/easyorange-application/src/main/java/com/cartethyia/easyorange/adapter/outbound/elasticsearch/ElasticsearchProductSearchIndexAdapter.java) |
| 限流降级拦截器 | `AiRateLimitInterceptor`（令牌桶 + stale 降级 + fail-open） | [easyorange-ai/interceptor/](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/interceptor/) |
| AI 可观测指标 | Spring AI 2.0 内置 Observation + Micrometer（原 `AiMetricsService` 已删除） | `spring-ai-autoconfigure-model-chat-observation` |
| Token 预算治理 | `@TokenBudget` AOP 切面 — **6 个 AI 场景全部接入**（7 处注解） | [easyorange-ai/budget/](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/budget/) |
| Prompt 版本化 | **6 个 YAML 模板**（ai_pricing / ai_copy / ai_review / ai_qa / auto_listing_system / auto_listing_visual） | [easyorange-ai/prompts/](../easyorange-backend/easyorange-ai/src/main/resources/prompts/) |
| **轻量 Agent 编排** | `AiSearchEnhancerAdapter` 4 路并行 Tool Calling（意图识别 + 标签 + 市场分析 + 建议问题），CompletableFuture 显式虚拟线程执行器 + 5s 单步超时降级 | [easyorange-ai/adapter/outbound/](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/adapter/outbound/AiSearchEnhancerAdapter.java) |
| AI 供应商 | DeepSeek (LLM) + 通义千问 VL (Vision) + DashScope (Embedding) | 改 `AiModelConfig` options（baseUrl/apiKey/model）即可切换 |
| **RAG 完整链路** | 文档摄入管线（解析→分块 500+overlap50→embed→ES `knowledge_docs` 索引，best-effort + 启动补索引）+ kNN+BM25 混合召回 → Java Cosine 重排 → [来源:标题] 引用溯源 | [KnowledgeIngestionService](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/KnowledgeIngestionService.java) + [KnowledgeRetrievalService](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/KnowledgeRetrievalService.java) + 种子文档 R__seed_knowledge_docs（kb-0001~0005） |
| **流式对话（SSE）** | `POST /api/ai/chat/stream` → SseEmitter + `ChatModel.stream`，事件协议 token/sources/done/error；预算手动检查（AOP 拦不住流式）+ 流式调用照常落调用日志 | [AiChatController](../easyorange-backend/easyorange-application/src/main/java/com/cartethyia/easyorange/adapter/inbound/web/controller/AiChatController.java) + `AiModelSupport.callTextStream` |
| **多轮 Agent + 记忆** | Redis 会话窗口（短期，TTL 24h）+ `eo_user_preference` 画像表（长期）+ 单步 ReAct 工具决策（knowledge_search + 偏好提取） | [AiChatService](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/AiChatService.java) + [ChatSessionStore](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/chat/ChatSessionStore.java) |
| **评估进 CI** | 金标准集 30 条（eval/golden-set.yaml）+ Judge 对照参考打分 + EvalGate 门禁（基线 4.0 - 0.3 卡 build，GoldenSetRegressionIT）+ hit@5/MRR 检索指标（eo_retrieval_metric） | [GoldenSetEvaluator](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/eval/GoldenSetEvaluator.java) + [AiJudge](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/AiJudge.java) |
| **反馈飞轮** | 👍/👎 入库（eo_ai_feedback）→ 管理端导出为金标准用例（导出即标记 exported）→ 人工审核合入评测集 | [AiFeedbackService](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/AiFeedbackService.java) + [GoldenSetExportService](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/GoldenSetExportService.java) |
| **语义缓存 + 模型路由** | 查询向量化 → Redis Hash 余弦相似度命中复用回答（阈值 0.92，超上限淘汰最旧，fail-open）；场景 → 模型 bean 配置路由 | [SemanticCacheService](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/SemanticCacheService.java) + [AiModelRouter](../easyorange-backend/easyorange-ai/src/main/java/com/cartethyia/easyorange/ai/service/AiModelRouter.java) |

### 1.4 前端工程化

| 指标 | 数值 / 实现 | 验证方式 |
|---|---|---|
| 前端测试文件数 | **111** | 同 §1.2（2026-08-19 复核） |
| 前端测试用例数 | **1,029** | `npm run test`（Vitest）实测全绿 — 2026-08-19 |
| 前端源码文件数 | **376** 个 tsx/ts + **41** 个 css | `find easyorange-frontend/src -name "*.tsx" -o -name "*.ts" \| wc -l`（2026-08-19 复核） |
| 共享 UI 组件 | **91** 个 | `find easyorange-frontend/src/components -name "*.tsx" \| wc -l`（2026-08-19 复核） |
| 管理端组件/页面 | **113** 个 | `find easyorange-frontend/src/admin -name "*.tsx" -o -name "*.ts" \| wc -l`（2026-08-19 复核） |
| Portal/Dialog/Drawer/Sheet 使用 | **106** 处 | `grep -rE "Portal\|Dialog\|Drawer\|Sheet" easyorange-frontend/src/admin --include="*.tsx" \| wc -l`（2026-08-19 复核） |
| TanStack Query hooks | **152** 处 | `grep -rE "useMutation\|useQuery\|useInfiniteQuery" easyorange-frontend/src --include="*.ts" --include="*.tsx" \| wc -l`（2026-08-19 复核） |
| a11y 属性 | **300** 处 | `grep -rE "aria-\|role=\|tabIndex" easyorange-frontend/src --include="*.tsx" \| wc -l`（2026-08-19 复核） |
| Lint | Biome 0 errors / 0 warnings | `npm run lint:check` |
| E2E | Playwright **6 spec / 41 用例**（auth / product / admin / checkout / **publish / order**，全 page.route mock 无后端依赖） | `npm run test:e2e` |

### 1.5 安全与可观测

| 指标 | 实现 |
|---|---|
| 认证 | Spring Security OAuth2 Resource Server + RSA JWT Access + Opaque Refresh（Redis、HttpOnly Cookie） |
| 限流 | `RateLimitFilter` — GET 走本地（200/60s/IP），写走 Redisson 分布式令牌桶（30/60s/IP），fail-open |
| 防重 | `RateLimitFilter` 短时间防连点（3s 间隔 + 请求体 hash） |
| 幂等 | `IdempotencyKeyFilter`（配置驱动，Idempotency-Key + 24h 响应缓存，非 2xx 不缓存） |
| 审计日志 | Spring Modulith **Outbox 模式** — `AuditLogAspect` 发布 `AuditLogEvent` → `AuditLogEventConsumer` 异步入库（崩溃时事件留在 `EVENT_PUBLICATION` 表，重启自动重发） |
| DLQ 三级重试 | `DlqRetryScheduler` 每 5 分钟扫描 DLQ → 重试次数 < 3 重投主队列 → 超过 3 次转储 `eo.dlq.terminal` 等待人工介入（DLQ 层固定轮询间隔，指数退避由主队列 RetryTemplate 承担） |
| 缓存 | Spring Cache 注解式 + Redis 单层（`RedisCacheConfig`）：统一 TTL（`easyorange.cache.default-ttl` 默认 30m）+ 写路径显式 `@CacheEvict` + `CacheErrorHandler` fail-open（Redis 故障读直查 DB、写放弃缓存）+ **三防**（null 结果缓存防穿透 / `@Cacheable(sync=true)` 单飞重建防击穿 / `JitterTtlRedisCacheWriter` TTL 抖动防雪崩，`easyorange.cache.ttl-jitter` 默认 0.1，2026-09-09）。手写多级缓存（L1 Caffeine + Pub/Sub 广播）已移除（2026-08-13） |
| CircuitBreaker | Resilience4j 已移除（2026-08-13，无消费者）；缓存故障降级由 `RedisCacheConfig` `CacheErrorHandler` fail-open 承担；AI 重试/并发隔离由 openai-java 客户端内置（[ADR-0008](adr/0008-ai-spring-ai-framework.md)） |
| 下单链路一致性 | **拒绝 Saga**（[ADR-0007](adr/0007-order-local-tx-over-saga.md)）：`OrderCommandHandler.handle(CreateOrderCommand)` 单一本地 `@Transactional`（订单/库存/支付/Outbox 原子提交）+ Redisson 分布式锁（`DistributedRedissonLockAdapter`，key=`eo:order:lock:product:{productId}`，按 productId 排序防死锁，10s 超时）；任一步失败整体回滚、抛 `OrderCreationException`，无补偿路径 |
| OpenAPI 3 | SpringDoc — `/v3/api-docs` + `/swagger-ui.html`，JWT securityScheme，Controller `@Tag` 分组 |
| XSS | CSP 头 (`default-src 'none'`) |
| 结构化日志 | Spring Boot 4 `StructuredLogEncoder`（logstash JSON 格式） |
| 健康检查 | `/actuator/health` + `/actuator/prometheus` |
| 指标采集 | Micrometer + Prometheus + Spring AI Observation + EventMetrics（事件接收/DLQ/时延） |
| **traceId 全链路传递** | Brave TracingFilter 提取 HTTP 头 → MDC → `MdcTaskDecorator` 传 @Async → `EventMetadataMessagePostProcessor` 注入 RabbitMQ message header → 消费者 decode 回 MDC → 日志 + metrics 统一关联；span 可视化：compose fullstack 部署 Zipkin（`ZIPKIN_ENDPOINT` 指向 zipkin:9411，2026-09-09） |

---

## 二、待实测指标（在原生 Linux/Mac 环境跑）

> 以下指标需要启动应用 + 压测工具才能拿到。JaCoCo 覆盖率报告已可在 WSL2 生成（`mock-maker-subclass` 模式 + `@{argLine}` 注入 agent），下方已填入实测数据；性能/缓存指标仍需启动应用采集。

### 2.1 测试覆盖率（JaCoCo）+ 变异测试（PIT）

```bash
# JaCoCo 覆盖率报告（各模块 target/site/jacoco/index.html）
./mvnw test jacoco:report

# PIT 变异测试（仅 domain 层，HTML 报告 target/pit-reports/index.html）
./mvnw -Ppit test-compile pitest:mutationCoverage
```

**实测数据**（2026-07-26 WSL2，v2 — Domain 工程质量提升后）：

| 范围 | 行覆盖率 | 分支覆盖率 | 说明 |
|---|---|---|---|
| domain 层（聚合根/领域服务/值对象） | **87.8%** ✅ 超越 80% 门禁（2026-08-19 重验，全模块 domain 聚合 1343/1530 行） | **70.5%** ✅ 超越 60% 门禁（416/590 分支） | 全模块 domain 聚合（order/product/payment/user/message/admin/common/favorite） |
| 全模块聚合（含 adapter/config/DTO） | 62.0% | 51.3% | adapter 靠 ArchUnit 守卫 + 类型系统，未追求全量行覆盖（2026-08-19 实测，较 7-26 提升系测试增量 + 死代码清理双因） |
| product 模块 domain（PIT 实测） | 92% | — | mutation 64% / test strength 74%（281 变异） |
| order 模块 domain（PIT 实测） | 93% | — | mutation 76% / test strength 78%（75 变异） |

> **测试策略说明**：单元测试聚焦 domain 层业务逻辑（状态机/不变量/校验），adapter/config 层靠 ArchUnit 架构守卫 + Spring 类型系统保证，不追求全量行覆盖（避免为样板代码写无价值测试）。PIT 变异测试作为"金标准"补充——行覆盖率只测"代码被执行过"，PIT 注入变异验证"测试能否发现缺陷"。JaCoCo 门禁（行≥80%/分支≥60%）在 `-Pci` profile，CI 用 `./mvnw -Pci verify` 启用；OWASP 依赖扫描拆到 `-Powasp` profile（CI 非阻断 security job，NVD 境外首次扫描 ~39 分钟不阻塞主门禁）；本地 `./mvnw test jacoco:report` 照常收集覆盖率并出报告，但默认不阻断（`-Djacoco.haltOnFailure=true` 可强制阻断）。domain 层行覆盖 87.8% 已超越门禁，分支覆盖 70.5% 持续优化中。

### 2.2 AI 调用观测（Spring AI Observation）

启动应用后访问：

```bash
# Prometheus 指标端点
curl http://localhost:8080/actuator/prometheus | grep spring_ai

# 关键指标（Spring AI 2.0 内置 Chat/Embedding 观测）
# - spring_ai_chat_model_call_duration_seconds{model="deepseek-chat",...}
# - spring_ai_embedding_model_call_duration_seconds{model="text-embedding-v3",...}
```

**待补数据**（需真实 LLM API key 才能采集；dev 环境密钥为占位/自动生成，AI 调用走 fail-open 降级，无法产生真实观测值）：
- LLM 调用 p99 延迟（目标 < 3s）
- 限流拒绝次数（按端点，`AiRateLimitInterceptor`）
- 单次 AI 决策平均 token 消耗（`@TokenBudget` 预估用量）

> 采集路径已就绪：`/actuator/prometheus` 暴露 Spring AI Observation 指标（`spring_ai_chat_model_call_duration_seconds` / `spring_ai_embedding_model_call_duration_seconds`），Grafana AI 大盘（infra/ 监控栈）可视化；配置真实 `DEEPSEEK_API_KEY` / `QWEN_VL_API_KEY` / `DASHSCOPE_API_KEY` 后按上述命令采集填入。

### 2.3 API 性能（JMeter / k6 / 等价并发脚本）

```bash
# 启动应用 + 基础设施（关闭全局限流，避免单机压测被 IP 限流截断）
docker compose -f compose.yaml up -d
RATE_LIMIT_FILTER_ENABLED=false ./mvnw spring-boot:run -pl easyorange-application

# 混合只读流量（列表 60% / 分类 20% / 搜索 20%，k6）
k6 run --vus 50 --duration 30s load-tests/product-list.js
# 搜索分项对比（ES 模式与 DB LIKE 模式各跑一轮；k6 不可用时的等价脚本见 load-tests/）
python3 load-tests/mixed-bench.py 50 30
python3 load-tests/search-bench.py 20 30 iPhone
```

**实测数据**（2026-08-15，WSL2 dev 栈，112 个商品 / 105 个在线，`RATE_LIMIT_FILTER_ENABLED=false`）：

| 场景 | 检索模式 | QPS | p50 | p95 | p99 | 失败率 |
|---|---|---|---|---|---|---|
| 混合只读流量（50 并发 30s） | DB LIKE 回退 | **1,025** | 28.7ms | 45.2ms | **52.6ms** | 0% |
| 混合只读流量（50 并发 30s） | ES（IK 分词） | **1,003** | 29.5ms | 45.7ms | **52.9ms** | 0% |
| 搜索专用（20 并发 30s，keyword=iPhone） | DB LIKE 回退 | **1,043** | 14.4ms | 19.6ms | **23.9ms** | 0% |
| 搜索专用（20 并发 30s，keyword=iPhone） | ES（IK 分词） | **959** | 15.2ms | 22.1ms | **28.0ms** | 0% |

**结论**：API p99 目标 < 500ms 达成（实测 53ms 级，留 ~10 倍余量）；112 行小数据集下 DB LIKE 回退与 ES 延迟相当（搜索 p99 24ms vs 28ms），ES 的优势需大数据量（ES 承担 IK 中文分词 + 语义检索，检索效果维度占优，见 §1.3 语义缓存/kNN 链路）。两种模式均 0% 失败率。

### 2.4 DORA 工程效能（CI/CD）

DORA 四指标（部署频率 / 变更前置时间 / 变更失败率 / MTTR）以真实发布节奏为前提，本项目为单人演示工程、无生产发布流水线，**不采集不造假**；以 CI 质量门禁作为等效工程效能证据（`docker compose up` 一键起全套 dev 栈，CI 全量校验见 §1.2）：

| 门禁 | 触发点 | 覆盖 |
|---|---|---|
| `backend-ci.yml` | push / PR | 11 模块构建 + 单测 + JaCoCo 行覆盖 ≥80% / 分支 ≥60% 门禁 + 跨模块 optional 依赖校验（TD-011） |
| `frontend-ci.yml` | push / PR | Biome 0 errors + Vitest 全量 |
| `pit-mutation.yml` | nightly | PIT 变异测试（mutation ≥60% / test strength ≥75% / coverage ≥70%） |
| `security-scan.yml` | nightly | OWASP 依赖漏洞扫描（NVD） |
| `qodana_code_quality.yml` | nightly | Qodana 静态分析 |

> 变更前置时间（commit → 门禁全绿）可在 Actions 页面按 workflow run 时长统计；若未来接入真实部署（K8s 已有 kustomize 清单），再按 DORA 口径补充四指标。

---

## 三、已知限制

### 3.1 WSL2 环境限制

| 限制 | 影响 | 项目应对 |
|---|---|---|
| Mockito inline mock maker 在 WSL2 下 ByteBuddy attach 失败 | 配置 `mock-maker-subclass` 后已跑通（测试全绿） | 原生 Linux / CI 无需该配置 |
| Docker 兼容性 | Testcontainers 集成测试无法跑 | 全量改为单元测试 + Mockito mock |
| Lombok JDK 25 弃用警告 | 启动打印 unsafe 警告 | 已配 `--sun-misc-unsafe-memory-access=allow` |

### 3.2 ES 是可选依赖

Elasticsearch 标记为可选，启动应用不强依赖 ES（`@ConditionalOnProperty`）。这降低部署门槛但意味着"ES 搜索"指标需在装好 ES 的环境单独采集。

---

## 四、维护与更新

| 触发条件 | 操作 |
|---|---|
| 新增模块 / Port / 消费者 | 更新 §1.1 表格 + 重跑验证命令 |
| 新增测试用例 | 重跑 `./mvnw test` + `npm run test`，更新 §1.2 总数 |
| 新增 ADR | 在 [doc/adr/](./adr/) 添加，编号递增 |
| 跑出实测数据 | 填入 §2 的"待补数据"，把对应行从"待补"改为实际数值 |
| 定位口径变更 | 同步 [README.md](../README.md) + [PRODUCT_DIRECTION.md](./PRODUCT_DIRECTION.md) + [AGENTS.md](../AGENTS.md) |

> 定位口径约定见 [AGENTS.md](../AGENTS.md)「定位」段。禁止使用"业务不是重点"等自我削弱叙事。下单链路统一口径为「拒绝 Saga：本地单事务 + 分布式锁 + Outbox（ADR-0007）」，测试总数取整 **2,400+**（精确实测见 §1.2，2026-08-19 收口）。
